---
name: cleanup-branches
description: マージ済みのブランチをリモート・ローカル・ワークツリーごと削除する。「不要なブランチを削除して」「ブランチを整理して」と依頼されたときに使う。未マージのブランチは削除せず一覧提示にとどめる。
---

このファイルは Claude Code 用の参照スタブ。スキルの実体は `.agents/skills/cleanup-branches/SKILL.md`。
実体を読み、その手順に従って実行せよ。編集は実体側だけに行う。
