{
  "name": "takagiaction",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WebGL 製トップダウン・アクションシューティング",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "skills:setup": "node scripts/manage-skill-stubs.mjs setup",
    "skills:check": "node scripts/manage-skill-stubs.mjs check",
    "dev": "vite",
    "build": "npm run skills:check && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "npm run skills:check && vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
