# Project Learnings

<!--
記法ルール:
- 1項目1洞察。複数の学びを1行に詰めない
- 各項目の先頭に日付を必ず入れる（例: - 2026-08-24: ...）
- 上4セクションは「生の観察」の置き場。Consolidated Principles には
  統合パスで抽出した原則だけを置く。両者を混ぜない
-->

## Patterns That Work
（効いたやり方・型）

- 2026-08-24: Windows で日本語を含む Agent Skill を `quick_validate.py` で検証するときは、`uv run --with pyyaml python -X utf8` で実行すると CP932 のデコードエラーを回避できる。
- 2026-08-24: 16×16 に BOX 縮小する生成画像では、重要な発光部を出力先の 1 ピクセル以上の太さで描かせ、焼き込み後のアトラスを実テクセルで検査すると潰れを検出できる。
- 2026-08-24: フラグメントシェーダに新しい色の規則を足す前に、アトラス全タイルを走査してその規則を誤って満たす texel がないか確認する。水色規則 `b>0.95 && g>0.25 && r==0` は 64 タイル中で誤爆ゼロだと分かり、設計を確定できた。
- 2026-08-24: 生成器が作る隙間と、そこを通す物のサイズは、生成器の式を再現するスクリプトで実測してから設計値を決める。柱の隙間が 16px でボスの判定 14px が実質通れないと分かり、案の 1 つが没になった。
- 2026-08-24: 半径が大きく変わる周回運動は、角速度ではなく線速度を一定にする。角速度一定だと半径の比がそのまま速度の比になり（半径 10 と 70 で 7 倍）、同じ相手が位置によって別の速さで動いて見える。
- 2026-08-26: 乱数を含む挙動のテストは、1 サンプルの閾値ではなく「N シード中 M 以上」の集計統計にする。閾値は現行実装と最も生き残る変異体の実測値の間に置き、両側の余裕を明記する。集計にすると `Math.sin` 等の数値ドリフトで軌道がずれても分布は変わらないので、決定的かつドリフトに強い。
- 2026-08-26: 台帳が git-ignored な場所にあると中断で消える。引継ぎメモを `docs/` 配下にコミットしておくと、台帳が消えても裁定・BASE コミット・人手待ち項目から再開できる。実際にこのセッションは台帳消失から引継ぎメモだけで復帰した。
- 2026-08-26: 完了した引継ぎメモは、設計上の結論だけをトピック別の `docs/` へ現在形で蒸留して削除する。進捗・再開手順・検証チェックリストまで正本へ写すと、設計書が作業履歴へ戻ってしまう。
- 2026-08-26: 「同じ処理を毎フレーム走らせてしまう」種の変異は、状態の値ではなく**発生回数の上限**で固定すると殺せる。60 フレーム回して符号の変化を数え、正しい実装 0 回・変異 60 回で赤くなった。最終状態だけを見る形にすると偶数回の反転が相殺されて素通りする。
- 2026-08-26: 素朴で明らかに間違った実装をいったん GREEN にしてから、それを殺すテストを次の RED として書くと、設計上の「なぜその置き場所なのか」がテストとして残る。反転抽選を `_repick()` に置く→毎フレーム反転を測る→満了の枝へ移す、の順で進めた。

## Mistakes to Avoid
（失敗と再発防止策）

- 2026-08-24: ImageGen の HEX 色指定は近似色になることがあり、BOX 縮小では周辺色も混ざるため、シェーダが厳密なチャンネル条件を見る色は焼き込み後に正規化して数値検証する。
- 2026-08-24: モックしたモジュールが内部に持つ状態（クールダウン・タイマー）は、そのモジュールをモックしているテストファイルでは検証できない。呼び出し側のテストで固定できるのは「呼び出しが起きること」までで、抑制の検証は別の場所に置く。
- 2026-08-24: BGM を複数持つ設計にするとき、通常再生への復帰はレート・フィルタ・音量だけでなく再生バッファも戻す。バッファを戻さないと、特殊BGMのまま次の場面が始まる。
- 2026-08-24: `docs/superpowers/plans/` のチェックボックスは実装が完了しても未更新のまま残ることがある（ボスの計画は 89 件すべて未チェックのまま実装済みだった）。完了判定はチェックボックスではなく、該当コードと `docs/` 直下への蒸留の有無で行う。
- 2026-08-26: 「壁の中に入らない」ことを主張するテストは「壁を回避して進める」ことを検証しない。ボスの滑り分岐を両方削除しても 450 件が全部緑のまま通り、実測では到達最大半径が 67.9px → 58.5px に劣化していた。障害物回避は必ず「到達できること」の側で固定する。
- 2026-08-26: 変異テストの適用範囲を実装ファイルの内部で止めると、呼び出し側の配線に穴が残る。`audio.ts` 内部は変異で裏が取れていたのに、`game.ts` と `entity-boss.ts` の関数呼び出し 3 箇所は消しても全緑だった。変異の候補には「その機能を成立させている最も外側の 1 行」まで挙げる。
- 2026-08-26: 他システムの数値をコメントで引用する前に、その値の次元（速度か加速度か）を確認する。「自機の約 130」を速度として比較した記述が 3 箇所あったが、128 は加速度で終端速度は 25.6px/s だった。設計の根拠がまるごと成り立っていなかった。
- 2026-08-26: 同じ 1 文がコードとドキュメントに複製されていると、片方を直した時点で自己矛盾が生まれる。直す前に同じ文言を grep して複製を数える。追尾弾の「壁で消える掃射と違い」は 3 箇所にあり、1 箇所だけ直して隣接コメントと正面衝突した。
- 2026-08-26: `Math.sign()` や符号だけを比較するテストは、両辺が `NaN` になると `Object.is` で一致して**偽の緑**になる。未定義の定数を割り算に使ったせいで `NaN` が全域へ伝播し、未実装のまま 2 件が通った。測定値が有限かつ 0 でないことを先に固定してから符号を比較する。
- 2026-08-26: タイマーを複数の分岐が書き換えるとき、満了の頻度をコードの読みで推測しない。「詰まると間隔が 0.4 秒へ縮むから抽選が 6 倍走る」と 2 回書いたが、実際はブロックの枝が毎フレーム間隔を戻すので満了自体が一度も来ない。頻度を主張する前に回して数える。
- 2026-08-26: マージ前レビューでは引継ぎメモの自己申告を信用せず、`git log` と現行コードで完了段階を照合する。ボス戦では Task 13 まで完了済みなのに「Task 1〜10 完了、Task 11 から再開」と残り、既に削除された計画書への再開指示まで含んでいた。

## Domain Knowledge
（業務・仕様に関する事実）

- 2026-08-24: ボス本体のタイル 45 は `m/q2.png` が唯一の原本であり、`tools/boss_tiles.py` は弾のタイル 46 だけを再生成してタイル 45 を変更しない。
- 2026-08-24: `entity_t._collides()` は `x`/`x+6`/`z`/`z+4` の四隅を見る 6×4 固定で、`this.w` を読まない。判定幅を広げたエンティティに壁衝突を持たせるには専用の `_collides()` が必要で、8px タイルでは 14px 幅が最大 3 タイル列にまたがるので四隅走査でも真ん中の列を見落とす。
- 2026-08-24: full-bright 規則が `b==0` のような厳密な等値条件を含むと、満たせる色域が 1 つの帯（赤〜橙〜黄）に縛られる。別系統の色を full-bright にするには規則を 1 本増やすしかない。
- 2026-08-24: `docs/gameplay.md` の闘技場の節は「座標そのものは仕様ではなく、不変条件は次の 3 つ」と明記している。docs に書かれた定数は、宣言された不変条件を満たす限り変更してよい。
- 2026-08-26: 自機の終端速度は 25.6px/s である。`player_speed()` が返す 128 は `entity-player.ts` で `t.ax`（加速度）へ代入され、`entity.ts` の積分 `vx += ax*dt - vx*min(f*dt,1)` の終端は `ax / friction`、friction は 5。離脱症状時は 96/5 = 19.2px/s。
- 2026-08-26: ボスの `_wander_timer` は、柱に完全に塞がれている間はブロックの枝が毎フレーム `boss_wander_retry_min` へ戻すため、満了の枝が一度も走らない。満了の枝に載せた挙動（周回の向きの反転）は、詰まっているあいだ起きない。
- 2026-08-26: `.claude/skills/` は `.agents/skills/` からの生成物（git-ignored）で、`npm test` と `npm run build` は先頭の `skills:check` がこれを検証する。clone 直後や別環境でテストが落ちたら実装ではなくリンク切れを疑い、`npm run skills:setup` で復旧する。
- 2026-08-26: `vi.mock('./dom', ...)` はこのリポジトリの確立した流儀で、`game.test.ts` / `minimap.test.ts` / `terminal.test.ts` が使っている。jsdom が無くても `dom.ts` に触るモジュール（`monologue.ts` など）は `./dom` と `./renderer` を差し替えれば Node で単体テストできる。

## Open Questions
（未解決・要調査）

- 2026-08-24: ボスを動かすと命中率が落ちるが、深度 5 の撃破時間（想定 20〜40 秒）がニコチンゲージ寿命（約 72 秒）を圧迫するかは試遊で未確認。
- 2026-08-24: 深度 25 以上のフェーズ 2 で同時 40 発超が一辺 184px の闘技場に飛ぶ可読性は試遊で未確認。
- 2026-08-26: 追尾弾（44px/s）から自機（終端 25.6px/s）が直線で逃げられないのは意図どおりか。想定の回避手段は「旋回半径ぶん外へ膨らませて追い越させる」「壁や柱へ誘導して消す」「寿命 5 秒まで耐える」の 3 つ。試遊で未確認。
- 2026-08-26: 追尾弾を激昂限定にしたことで、前半の同時弾数が深度 5 で約 12 から約 8 へ落ちた。前半の手応えが足りるかは試遊で未確認。緩ければ次に触る摘みは前半の発射の刻み（0.18 rad）か砲塔の角速度（0.5 rad/s）で、深度スケールの軸（砲口の本数）ではない。
- 2026-08-26: ボスは目標半径の上限 70px により柱リング（内縁 60px・外縁 76px）へ 10px 食い込むだけで、柱の外側へは出ない。柱を越えて外周まで回らせたいなら `boss_orbit_radius_max` を上げる判断が要るが、「壁に張り付くと自機が背後を取れない」という既存の理由と衝突する。

## Consolidated Principles
（統合パス専用。ここに直接追記しない）
