# Project Learnings

<!--
記法ルール:
- 1項目1洞察。複数の学びを1行に詰めない
- 各項目の先頭に日付を必ず入れる（例: - 2026-08-24: ...）
- 上4セクションは「生の観察」の置き場。Consolidated Principles には
  統合パスで抽出した原則だけを置く。両者を混ぜない
-->

## Patterns That Work
（効いたやり方・型）

- 2026-08-24: 小さなアトラス素材は、重要部を出力先の 1 ピクセル以上で生成し、焼き込み後の全 texel を走査してシェーダ条件の誤爆と色の潰れを数値検証する。
- 2026-08-24: 生成器が作る隙間と通過物のサイズは、生成式を再現するスクリプトで実測してから設計値を決める。柱の隙間 16px に判定幅 14px のボスは実質通れないと分かり、案を早期に棄却できた。
- 2026-08-26: 乱数を含む挙動のテストは、1 サンプルの閾値ではなく「N シード中 M 以上」の集計統計にする。閾値は現行実装と最も生き残る変異体の実測値の間に置くと、数値ドリフトに強くなる。
- 2026-08-26: 毎フレーム再実行されるバグは、最終状態ではなく発生回数の上限で固定する。偶数回の反転が相殺されるケースでも回帰を検出できる。
- 2026-08-26: 素朴な誤実装を一度 GREEN にし、それを殺すテストを次の RED にすると、処理の置き場所に関する設計理由がテストとして残る。
- 2026-08-26: フレーキーなテストは pass/fail の再現率ではなく、タイムアウト予算の消費率で危険度と改善量を測る。
- 2026-08-26: 数分かかる計測は生データをファイルへ保存し、解析を別プロセスに分ける。解析が失敗しても高価な計測をやり直さずに済む。
- 2026-08-26: キャッシュ温度が効く A/B 比較は A→B→A→B と交互に複数巡し、実装差と実行順の影響を分離する。
- 2026-08-27: 中断作業の再開は、PR のマージ状態と `git diff origin/main...HEAD` を確認し、差分が空なら直近ブランチ間で欠けたコミットを探す。
- 2026-08-27: 全画面 DOM UI は `vi.hoisted()` で最小の `document` を `globalThis` に置けば、jsdom を追加せず Node でテストできる。
- 2026-08-27: 入力ハンドラが複数系統ある場合は、実機と同じ登録順でイベントを両方へ配ると、片方の状態更新を含むバグを再現できる。
- 2026-08-28: 別種のレビュー結果は出揃ってから一度の修正単位へまとめると、同じファイルの再読込と重複検証を減らせる。
- 2026-08-28: 共有ワークツリーで別プロセスの編集を疑ったときは mtime を確認する。コンフリクトマーカーが直後に消えた事例では、更新時刻から別セッションの介在を確定できた。
- 2026-08-28: 「DOM は初回だけ構築する」という不変条件は、テスト用要素の `innerHTML` setter で書き込み回数を数え、初回 import 後の値を基準に再表示で増えないことを固定できる。
- 2026-08-28: ブランチのマージ済み判定は `git fetch --prune` の後に `git branch -a --merged origin/main` と基準を明示して行う。引数なしの `--merged` はローカル HEAD 基準なので、ローカル main が遅れていると（この回は 59 コミット）マージ済みブランチが未マージに見え、消せるものを消し損ねる。
- 2026-08-28: 生成画像へ CSS の動的要素（火種・煙）を重ねるときは、画像を要素と同じ縦横比に切り出して `aspect-ratio` を一致させ `background-size: 100% 100%` で嵌める。cover だと窓の比で % 座標が流れ、画像内座標に固定できない。重ねる位置は生成後に拡大クロップで実測してから CSS に書く。
- 2026-08-28: アプリ内 Browser ペインが非表示だと画面が composite されず screenshot が全て失敗するので、実 Chrome（claude-in-chrome）で同じ URL を開く。実 Chrome でもタブが背面（`document.hidden`）だと setTimeout が絞られて起動フローは進まないが、screenshot は CDP が強制コンポジットするので撮れる ― 絵は撮れるが時間は進まない、と分けて考える。
- 2026-08-28: 「コンフリクトを解消して」と言われて作業ツリーがクリーンなら、`git merge --no-commit --no-ff origin/main` で衝突を顕在化させてから解消する。状態の推測より試行マージのほうが対象と範囲が確実に分かる。
- 2026-08-28: 変異が生き残ったとき、まずテストの不足ではなく実装の冗長性を疑う。ボス階の照明で 2 つ目のガードが殺せなかったのは、1 つ目のガードが同じ条件を既に満たしていたためで、正解はテスト追加ではなく分岐の削除だった。
- 2026-08-28: 状態のリセット漏れは、同じ分岐に入る 2 度目の機会まで進めないと観測できない。ボス階の明転を通常フロアで検証しても、進捗を 0 に固定する実装のため素通りした。
- 2026-08-28: Vite dev の実行中モジュールは `import('/source/xxx.ts')` で同一インスタンスが取れる。関数を直呼びすれば、rAF が回らない環境でも実 GL コンテキストへの uniform の往復を検証でき、モックを使ったユニットテストの数値を実物へ同じ入力で流して同値を確認でき、起動フロー（audio_init 待ち）を迂回して `death_screen_show(null, () => {})` のように画面だけを出せる。
- 2026-08-28: 「速くしたのに進まない」は経路長と正味変位を分けて測る。清掃ドローンでは経路長が 164→113 と減ったのに正味変位は増え、動きを方向転換に浪費していたと確定できた。支配要因は速度定数ではなく方向の再抽選頻度だった。
- 2026-08-28: 集計統計の閾値は、変異体を 1 つずつ実測して最も生き残るものを特定してから置く。実装 11/20 に対し半径の変異が 9/20 まで残ったため、統計を締めるのではなく半径専用の決定的なテストを足す判断ができた。
- 2026-08-28: 定数の変更が既存のしきい値と衝突したら、数値ではなく意味を移せないか探す。150 を逃走速度から巡回速度へ移すと、docs の「脚力 Lv4 が 150 を追い越す」を文字どおり残したまま体験だけを変えられた。
- 2026-08-28: CSS アニメーションの見た目は `getAnimations()` で pause し、`currentTime` を山と谷に固定してから撮る。位相が確定するので、rAF が絞られる環境でも「点滅している」ことを 2 枚の静止画で示せる。
- 2026-08-28: 状態クラスごとの適用可否は、DOM のクラスを直接付け替えて `getAnimations()` の中身を数えれば確認できる。実 UI を全状態へ遷移させずに、静止すべき 4 クラスと動くべき 1 クラスを 1 回で押さえられる。

## Mistakes to Avoid
（失敗と再発防止策）

- 2026-08-24: モックしたモジュール内部のクールダウンやタイマーは、そのモジュールをモックしている呼び出し側テストでは検証できない。内部の抑制は所有モジュール側で固定する。
- 2026-08-24: 複数 BGM から通常再生へ戻すときは、レート・フィルタ・音量だけでなく再生バッファも戻す。
- 2026-08-26: 「壁の中に入らない」テストは「壁を回避して進める」ことを保証しない。障害物回避は到達可能距離の側でも固定する。
- 2026-08-26: 変異テストを実装モジュール内部で止めると、呼び出し側の配線を削除しても全緑になりうる。機能を成立させる最も外側の呼び出しまで変異候補に含める。
- 2026-08-26: 他システムの数値を根拠に使う前に次元を確認する。自機の 128 は速度ではなく加速度で、終端速度は 25.6px/s だった。
- 2026-08-26: `Math.sign()` などの比較は両辺が `NaN` だと偽の緑になる。有限かつ非ゼロであることを先に検証する。
- 2026-08-26: タイマー分岐の実行頻度をコードの印象で断定しない。ブロック分岐が毎フレーム値を戻すため、満了分岐が一度も走らない事例があった。
- 2026-08-26: Vitest で `vi.resetModules()` と動的 `import()` を毎テスト行うと変換コストが per-test の時間予算に乗る。トップレベル import へ移すと 55〜577ms まで短縮できた。
- 2026-08-26: ローカルのテスト失敗を CI の問題と決めつけず、CI の履歴と実行環境差を先に確認する。音声テストのタイムアウトは Windows の高並列環境だけで再現した。
- 2026-08-26: Git Bash の `/tmp` は Windows ネイティブの Node には `C:\tmp` として解釈される。Node へ渡すパスは Windows から解決できる形式にする。
- 2026-08-26: シェルをまたいで here-string や多段エスケープを流用しない。構文が複雑になったらスクリプトファイルへ切り出す。
- 2026-08-27: 早期 return を追加するときは、その下で減算しているタイマーを列挙する。被弾点滅タイマーが死体ゲートより下で凍結した事例がある。
- 2026-08-27: singleton の状態に依存するテストは前提値を明示する。`game_running` ガード追加時、既定値に依存した describe だけが失敗した。
- 2026-08-28: 背景ジョブの完了を記録上の status だけで待たない。プロセスの生存も確認し、`running` のまま沈黙死するケースを終端として扱う。
- 2026-08-28: Codex に巨大な diff や数千行の文書を一度に読ませない。ファイルへ保存し、範囲を切って段階的に読む。
- 2026-08-28: Windows で別ディレクトリの `node_modules` を junction として作業ツリーへ置いたまま再帰削除すると、リンク先まで失う危険がある。削除前に junction 自体を解除し、リンク先が残っていることを確認する。
- 2026-08-28: 色や比率を持つ定数の新しい値を決める前に、その比に依存している既存の不変条件を docs から全件検索する。環境光を明るくする値が「青は緑の 2 倍」を崩し、非常口の緑タイルの焼き色の根拠を壊しかけた。
- 2026-08-28: Python の `io.open(p,'w').write(expr)` は open が先に評価されるため、expr が例外を投げるとファイルが空のまま残る。破壊的な書き込みは read を済ませてから開くか、一時ファイル経由にする。
- 2026-08-28: 同じ意味の合図をモードごとに別のアニメーションで持たせない。「買える」に待機中 2.8 秒の明滅と強化モード 2.4 秒の呼吸を当てていたが、速さと強さの差が意味の差として読まれ、どちらも伝わらなかった。1 つの意味には 1 本を当て、状態は止める／止めないで表す。

## Domain Knowledge
（業務・仕様に関する事実）

- 2026-08-24: ボス本体のタイル 45 は `m/q2.png` が唯一の原本であり、`tools/boss_tiles.py` は弾のタイル 46 だけを再生成する。
- 2026-08-24: `entity_t._collides()` は 6×4 固定の四隅を見て `this.w` を読まない。幅広エンティティには専用の衝突判定が必要である。
- 2026-08-24: full-bright 規則が厳密なチャンネル等値条件を含むと色域が一帯へ縛られる。別系統の色を光らせるには規則を追加する必要がある。
- 2026-08-24: `docs/gameplay.md` の闘技場座標は仕様ではなく、明記された不変条件を満たす限り変更できる。
- 2026-08-26: 自機の終端速度は通常 25.6px/s、離脱症状時 19.2px/s である。`player_speed()` の 128 / 96 は加速度で、friction は 5。
- 2026-08-26: ボスが柱に完全に塞がれている間は `_wander_timer` が毎フレーム retry 最小値へ戻るため、満了分岐は走らない。
- 2026-08-26: `.claude/skills/<name>/SKILL.md` は `.agents/skills/` の正本を読ませる Git 追跡対象の参照スタブであり、管理手順の正本は `.agents/rules/skill-management.md` である。
- 2026-08-26: `vi.mock('./dom', ...)` はこのリポジトリで jsdom を使わず DOM 依存モジュールを単体テストする確立した方法である。
- 2026-08-27: 自機の被弾点滅は `_last_damage` が正の間、6 フレーム中 4 フレーム描画する。更新と描画はいずれも `state.dying || !state.game_running` で死体を除外する。
- 2026-08-27: キー入力は `document.onkeydown` と全画面 UI の `addEventListener('keydown')` が同居する。画面側の `preventDefault()` は `keys` 更新を止めないため、画面を抜ける処理がキー状態を戻す。
- 2026-08-27: `game_tick` は死亡終端で `dying` を落として `game_running` を落とした同じフレームにもエンティティを更新するため、死体ゲートには両方が必要である。
- 2026-08-28: `animation-fill-mode: both` は animation origin の値を通常宣言より優先し、長寿命の `entering` クラスでは休止状態を到達不能にする。基底値が明示された `to` keyframe には `backwards` を使う。
- 2026-08-28: reduced-motion でアニメーション尺だけを 1ms にしても無限アニメは毎秒 1000 回繰り返す。`animation-iteration-count: 1` も必要である。
- 2026-08-28: `設計書 §N` 形式のコードコメントは、参照先の作業用設計書を削除した後も残すのがこのリポジトリの現行作法である。
- 2026-08-28: 本体の Vite 6 と Vitest 2.1 内部の Vite 5 が同居する。サンドボックスで設定を読めない場合、ビルドは `--configLoader runner`、テストは `startVitest()` の `config: false` とインライン設定で迂回する。
- 2026-08-28: 起動フローは audio_init の音生成が終わるまで `document.onclick` を張らない。生成が遅い環境（複数タブ同時など）ではクリックが無視され「起動中...」のまま数十秒待つことがあり、背面タブでは setTimeout が絞られて事実上終わらない。死亡画面の検証は onclick が付いたのを確認してからクリックする。
- 2026-08-28: 闘技場は一辺 184 単位あるが、固定カメラに一度に映るのは自機周囲およそ ±50 単位である。「フロア全体を一望」は霧ではなく画角の制約で、カメラを引かない限り成立しない。
- 2026-08-28: 環境光と霧の遠距離はシェーダ定数ではなく uniform（`amb` / `ff`）で、値の正本は `boss-light-model.ts` である。`renderer.ts` の `lighting` は `camera` と同じ受け渡し口にすぎない。
- 2026-08-28: LEARNINGS.md は全ブランチが同じセクション末尾へ追記するため、main とのマージでほぼ必ず内容衝突する。追記同士なので解消は常に両側保持で、`<<<<<<<` / `=======` / `>>>>>>>` の 3 行を削除して件数が一致することを確認すればよい。
- 2026-08-28: `entity_t._update()` の壁判定は軸ごとに独立で、斜め移動で片軸が壁に当たってももう片方は滑り続ける。`_did_collide()` はその片軸接触でも呼ばれるので、これを「行き止まり」として扱うと壁沿いの移動が壊れる。
- 2026-08-28: 自機のプラズマは速度 96px/s の固定値。まっすぐ逃げる敵の終端速度がこれを超えると弾が永久に追いつかず撃破不能になるので、逃げる敵の速度設計の上限になる。
- 2026-08-28: 通常フロアは 64×64 タイル × 8 単位 = 512×512 単位。カメラに映るのは自機周囲およそ ±50 単位なので、フロア全体は画角のおよそ 5 倍ある。
- 2026-08-28: 全画面 UI が閉じるときに戻すのは `key_spare`(E) / `key_swap`(Tab) だけだが、`document.onkeyup` は `input_init()` が 1 度張ったきり誰も外さない。決定に使ったスペース（`key_shoot`）は押しっぱなしの間だけ次の状態へ持ち越し、離せば戻る。

## Open Questions
（未解決・要調査）

- 2026-08-24: 深度 5 のボス撃破時間がニコチンゲージ寿命を圧迫するかは試遊で未確認。
- 2026-08-24: 深度 25 以上のフェーズ 2 で同時 40 発超になる弾幕の可読性は試遊で未確認。
- 2026-08-26: 追尾弾から自機が直線で逃げられない難度が意図どおりかは試遊で未確認。
- 2026-08-26: 追尾弾を激昂限定にした後の前半の手応えは試遊で未確認。
- 2026-08-26: ボスを柱の外側まで回らせるべきかは、「壁際では背後を取れない」という既存制約との裁定が必要である。
- 2026-08-28: 死亡画面の段階速度、部位演出、退場演出の知覚上の手触りは試遊で未確認。
- 2026-08-28: パネル表示中に地下へ戻れない状態機械の裁定が意図どおりか未確認。
- 2026-08-28: ボス階の明転の数値（環境光 1.5 倍・霧の遠距離 200・尺 3 秒）が、ボスの動きの可読性と闘技場の圧迫感の両立として妥当かは試遊で未確認。
- 2026-08-28: 清掃ドローンの巡回 30px/s・逃走 60px/s が、収入源を見失う頻度と弾で削り切る難度の両面で妥当かは試遊で未確認。
- 2026-08-28: 待機中の視線の配分（「買える部位」の 0.5 秒周期の点滅と、「地下へ潜る」ボタンの火種・煙の強さ）が試遊で適切に感じられるかは未確認。淡い明滅（2.8 秒周期・色を 55% に減光）は気づけないと判明して作り替えた側なので、今度は強すぎないかを見る。

## Consolidated Principles
（統合パス専用。ここに直接追記しない）

- 設計書、レビュー、引継ぎメモ、コメントの主張を採用するときは、現行コード・履歴・実測のうち該当する証拠で照合せよ。
- 挙動をテストするときは最終状態だけでなく、機能を成立させる最外周の配線、発生回数、到達可能性、有限性も観測せよ。
- ある不具合のパターンを見つけたときは、同じ式・文言・CSS 指定・状態ゲートを全件検索し、同種箇所を同じ修正単位で扱え。
- 高価な計測は生データを永続化し、A/B の実行順を交互にして、計測と解析を分離せよ。
- 生成物や同期物を検証するときは存在確認ではなく期待値との完全一致を見て、代表的な失敗系を実際に壊して検証せよ。
- 画面が composite されず時間も進まない環境では検証を諦めず、実行中モジュールの直呼びと DOM API（`getAnimations()` の位相固定、クラスの付け替え）で状態を確定させ、絵が要るときだけ実ブラウザで撮れ。
- reduced-motion を実装するときは尺だけでなく反復を止め、視覚演出に対応する JS の待ち時間も同じ設定で畳め。
- 共有ワークツリーで編集・削除するときは mtime と Git 状態を確認し、symlink や junction のリンク先を破壊しないことを確かめよ。
- シェルや OS の境界をまたぐコマンドは各環境のパス・引用・エスケープ規則で検証し、複雑ならスクリプトファイルへ切り出せ。
