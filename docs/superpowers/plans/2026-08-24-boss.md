# ボス戦（灰皿撤去ユニット）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 深度が 5 の倍数のフロアを 1 部屋の闘技場にし、中央の灰皿に居座るボスを倒して初めて一服できるようにする。撃破報酬は恒久強化 1 段（次のランから有効）と押収品コンテナ 1 個（100%）。

**Architecture:** 生成器に `build_arena()` の分岐を足し、`level_layout_t` に `boss` / `boss_spin` を追加する。ボスは `entity-boss.ts` の 2 クラス（本体・弾）で、数値と掃射の幾何は葉モジュール `boss-model.ts` に分けて Node でテストする。報酬は `state.boss_levels` に積み、`run_end()` がヤニと同じブロックで `meta.levels` へ書く。

**Tech Stack:** TypeScript 5.7 / Vite 6 / Vitest 2 / WebGL（自前レンダラ）/ Pillow（アトラス焼き込み、`uv run` 経由）

**Spec:** `docs/superpowers/specs/2026-08-24-boss-design.md`

## Global Constraints

- **後方互換レイヤーを作らない。** 置き換えたら古い経路は消す（`AGENTS.md`）
- **最もシンプルな実装を選ぶ。** 呼び出し元が 1 か所のものに抽象化を作らない。ただし要件は完全に満たす（`AGENTS.md`）
- **Python は必ず `uv run` 経由で実行する。** `uv run --with pillow python tools/xxx.py`（`AGENTS.md`）
- **`entity.ts` は `entity_t` のサブクラスを宣言するモジュールへ到達してはならない。** これが唯一の循環参照の不変条件で、`entity-*.ts` どうしの循環は許容されている（`docs/architecture.md`）
- **`_init()` には基底クラス（`entity_t`）のフィールドしか代入できない。** サブクラス固有の状態はフィールド初期化子に書き、生成側が `new` の後に代入する（`useDefineForClassFields`。`docs/architecture.md`）
- **画像は静的 import のみ。** 文字列連結の URL は Vite が検出できず本番で 404（`docs/architecture.md`）
- **新しい sonantx の音色を足さない。** 既存の効果音を組み合わせる
- **`state.ts` は実行時 import を一切持たない。** 型は `import type` で受ける
- **生成器（`level-generator.ts`）は `renderer` / `dom` / `audio` を import しない**
- **コメントと識別子は既存の流儀に合わせる。** コメントは日本語で「なぜそうなっているか」を書き、処理の逐次説明は書かない
- 検証コマンドは `npm test`（Vitest）と `npm run typecheck`（`tsc --noEmit`）

## ファイル構成

**新規**

| ファイル | 責務 |
| --- | --- |
| `source/boss-model.ts` | ボスの数値と掃射の幾何。実行時 import を一切持たない葉モジュール |
| `source/boss-model.test.ts` | 上のテスト |
| `source/entity-boss.ts` | `entity_boss_t`（本体）と `entity_boss_plasma_t`（弾） |
| `source/upgrade-rows.ts` | 恒久強化 6 行の表示定義。死亡画面とボス報酬が共有する |
| `source/boss-reward-model.ts` | 報酬の実効段の計算。葉モジュール |
| `source/boss-reward-model.test.ts` | 上のテスト |
| `source/boss-reward.ts` | 撃破報酬のダイアログ（DOM） |
| `source/boss-reward.css` | 上のスタイル |
| `tools/boss_tiles.py` | アトラス 45（暫定）・46（弾）の焼き込み |
| `images.md` | ボス本体の画像生成プロンプト。焼き込んだら削除する |

**変更**

| ファイル | 変更 |
| --- | --- |
| `source/entity.ts` | 衝突判定の幅 `w = 9` を追加 |
| `source/game.ts` | 衝突ループの幅、`state.paused` への改名、ボスの生成、`run_end()` の報酬適用、ボス階の到達通知 |
| `source/state.ts` | `equipping` → `paused` の改名、`boss_alive` / `boss_levels` の追加 |
| `source/level-generator.ts` | `build_arena()`、`level_layout_t` の 2 フィールド、`take_from()` の共通化 |
| `source/meta.ts` | `meta_grant()` |
| `source/entity-plasma.ts` | ボスを被弾対象に追加 |
| `source/entity-player.ts` | 薙ぎの対象にボスを追加（一撃必殺は除外） |
| `source/entity-smoking-area.ts` | ボスが生きている間は一服させない |
| `source/entity-container.ts` / `source/equip-screen.ts` | `state.paused` への改名 |
| `source/death-screen.ts` | 行定義を `upgrade-rows.ts` へ移す |
| `source/monologue.ts` | ボス階のセリフ 2 プール |
| `tools/atlas.py` | `TILE_RANGE` を 46 まで |
| `docs/*.md` | 6 ファイル |

---

### Task 1: 衝突判定の幅をエンティティに持たせる

見た目だけ大きいボスを作ると、`game.ts` の衝突ループがハードコードしている 9 のせいで輪郭に撃った弾がすり抜ける。既定値を現行と同じ 9 にしてフィールド化する。

**Files:**
- Modify: `source/entity.ts`（`h = 5` の直後）
- Modify: `source/game.ts`（衝突ループの AABB 判定）
- Test: `source/game.test.ts`（末尾に describe を追加）

**Interfaces:**
- Consumes: なし
- Produces: `entity_t.w: number`（既定 9）。Task 6 のボスが `_init()` で 14 に広げる

- [ ] **Step 1: 失敗するテストを書く**

`source/game.test.ts` の末尾に追加する。`import { entity_t } from './entity'` を既存の import 群へ足すこと。

```ts
describe('衝突判定の幅', () => {
  class probe_t extends entity_t {
    hits = 0
    override _check(_other: entity_t): void { this.hits++ }
  }

  beforeEach(() => { start_run() })

  // フロアの外の虚空（タイル値 0）に置く。壁ではないので _collides に
  // 引っかからず、他のエンティティとも重ならない
  it('既定の幅 9 では 9 離れると当たらない', () => {
    const a = new probe_t(490, 0, 490, 0, 0)
    new probe_t(499, 0, 490, 0, 0)
    step()
    expect(a.hits).toBe(0)
  })

  it('幅を広げた側は広げた分だけ当たる', () => {
    const a = new probe_t(490, 0, 490, 0, 0)
    a.w = 14
    const b = new probe_t(499, 0, 490, 0, 0)
    step()
    expect(a.hits).toBe(1)
    expect(b.hits).toBe(1)
  })
})
```

- [ ] **Step 2: 失敗を確認する**

Run: `npm test -- game.test.ts`
Expected: FAIL。`a.w = 14` が型エラー（`Property 'w' does not exist on type 'probe_t'`）になるか、2 本目のテストが `hits` 0 で落ちる

- [ ] **Step 3: `entity.ts` にフィールドを足す**

`source/entity.ts` の `h = 5` の直後に追加する。

```ts
  h = 5
  // 衝突判定の一辺（ワールド単位）。game.ts の衝突ループが AABB の幅として
  // 読む。既定 9 は原作から引き継いだ値で、これを広げるのは見た目が通常の
  // スプライトより大きいボスだけ（見た目だけ大きくして 9 のまま残すと、
  // 輪郭に撃った弾がすり抜けて「当たらない」体験になる）
  w = 9
```

- [ ] **Step 4: `game.ts` の衝突ループを幅で書く**

`source/game.ts` のエンティティループ内、`if (!(` から始まる AABB 判定を置き換える。

```ts
        if (!(
          e1.x >= e2.x + e2.w ||
          e1.x + e1.w <= e2.x ||
          e1.z >= e2.z + e2.w ||
          e1.z + e1.w <= e2.z
        )) {
```

- [ ] **Step 5: テストと型検査を通す**

Run: `npm test && npm run typecheck`
Expected: 全 PASS。既定値が現行のハードコード値と同じ 9 なので、既存テストの結果は 1 件も変わらない

- [ ] **Step 6: commit**

```bash
git add source/entity.ts source/game.ts source/game.test.ts
git commit -m "衝突判定の一辺をエンティティのフィールドにする"
```

---

### Task 2: `state.equipping` を `state.paused` に改名する

ポーズはこの先 2 種類（装備の開封・ボス報酬）になる。フラグを 2 本持つと `equipping || leveling` が 3 か所に散るので、汎用の 1 本にする。純粋な改名で挙動は変わらない。

**Files:**
- Modify: `source/state.ts`, `source/game.ts`, `source/entity-container.ts`, `source/equip-screen.ts`
- Test: `source/game.test.ts`, `source/entity-container.test.ts`（既存の参照を追随させる）

**Interfaces:**
- Consumes: なし
- Produces: `state.paused: number`。Task 11 のダイアログが立てて下ろす

- [ ] **Step 1: 参照を洗い出す**

Run: `grep -rn "equipping" source/`
Expected: 6 ファイル・17 件（`state.ts` 1、`game.ts` 4、`entity-container.ts` 2、`equip-screen.ts` 2、`game.test.ts` 6、`entity-container.test.ts` 2）

- [ ] **Step 2: `state.ts` のフィールドを改名する**

```ts
  // ポーズ中は 1。ゲーム内で唯一の時間停止で、game_tick が time_elapsed を
  // 0 にしてエンティティの更新と衝突判定を飛ばす。立てるのは押収品コンテナの
  // 開封ダイアログ（equip-screen.ts）とボス撃破の報酬ダイアログ
  // （boss-reward.ts）の 2 つ
  paused: 0,
```

- [ ] **Step 3: 残り 5 ファイルを機械的に置換する**

`state.equipping` → `state.paused` に置き換える。`game.ts` のコメント中の「開封ダイアログ中は時間を止める」は「ポーズ中は時間を止める」にし、括弧内の説明に「（開封ダイアログとボス報酬の 2 か所が立てる）」を足す。

Run: `grep -rn "equipping" source/`
Expected: 0 件

- [ ] **Step 4: テストと型検査を通す**

Run: `npm test && npm run typecheck`
Expected: 全 PASS。挙動は一切変わらない

- [ ] **Step 5: commit**

```bash
git add source/state.ts source/game.ts source/entity-container.ts source/equip-screen.ts source/game.test.ts source/entity-container.test.ts
git commit -m "state.equipping をポーズ全般の state.paused に改名する"
```

---

### Task 3: アトラスのタイル 45・46 を用意する

弾（46）は絵ではなく単色の点なので、`tools/slash_tiles.py` と同じくコードが原本になる。ボス本体（45）は生成した絵ができるまでの暫定プレースホルダを焼き、Task 14 で本物に差し替える。

**Files:**
- Create: `tools/boss_tiles.py`
- Create: `images.md`
- Modify: `tools/atlas.py`（`TILE_RANGE`）
- Modify: `m/q2.png`（生成物）

**Interfaces:**
- Consumes: なし
- Produces: アトラスのタイル 45（ボス本体）と 46（弾）。Task 6 が `push_quad` / `push_sprite` の tile 引数に使う

- [ ] **Step 1: 空きタイルを確認する**

```bash
uv run --with pillow python -c "
from PIL import Image
im = Image.open('m/q2.png').convert('RGBA')
n = im.size[0] // 16
used = [i for i in range(n) if any(p[3] for p in im.crop((i*16,0,(i+1)*16,16)).get_flattened_data())]
print('used', used[-1], 'free from', used[-1] + 1)
"
```
Expected: `used 44 free from 45`

- [ ] **Step 2: `tools/boss_tiles.py` を書く**

```python
"""m/q2.png のタイル 45・46 を焼き込む。

使い方: uv run --with pillow python tools/boss_tiles.py

46（ボスの弾）は絵ではなく単色の点なので、tools/slash_tiles.py と同じく
コードが原本になる。色は既存の敵の赤 (255,66,0) — 蜘蛛（27）とセントリー
（32）の目に使われているまさにその色で、フラグメントシェーダの full-bright
規則（r>0.95 && g>0.25 && b==0）を満たす。この規則を満たす texel はライトも
霧も通さないので、弾は push_light() なしで等しく明るく見える。ボスの弾は
同時に最大 26 発飛び、max_lights = 16 には載せられないため、これが唯一の経路。

45（ボス本体）は暫定のプレースホルダである。生成した絵ができたら
tools/atlas.py で焼き直し、この関数ごと消す（押収品コンテナ 42 と同じ手順）。
"""
from pathlib import Path

from PIL import Image

ATLAS = Path(__file__).resolve().parent.parent / 'm' / 'q2.png'
TILE_SIZE = 16

BOSS_TILE = 45
BULLET_TILE = 46

# full-bright 規則を満たす 2 色。外周は敵の赤、芯だけ橙に寄せて厚みを出す
BULLET_EDGE = (255, 66, 0)
BULLET_CORE = (255, 150, 0)
BULLET_RADIUS = 3.2
BULLET_CORE_RADIUS = 1.6

# プレースホルダの機体色と、full-bright の目
BODY = (96, 102, 108)
EYE = (255, 66, 0)


def clear(pixels, tile: int) -> None:
    ox = tile * TILE_SIZE
    for y in range(TILE_SIZE):
        for x in range(TILE_SIZE):
            pixels[ox + x, y] = (0, 0, 0, 0)


def bake_bullet(pixels) -> None:
    ox = BULLET_TILE * TILE_SIZE
    clear(pixels, BULLET_TILE)
    for y in range(TILE_SIZE):
        for x in range(TILE_SIZE):
            dx = x - 7.5
            dy = y - 7.5
            d = (dx * dx + dy * dy) ** 0.5
            if d <= BULLET_CORE_RADIUS:
                pixels[ox + x, y] = (*BULLET_CORE, 255)
            elif d <= BULLET_RADIUS:
                pixels[ox + x, y] = (*BULLET_EDGE, 255)


def bake_boss_placeholder(pixels) -> None:
    ox = BOSS_TILE * TILE_SIZE
    clear(pixels, BOSS_TILE)
    for y in range(2, 15):
        for x in range(2, 14):
            pixels[ox + x, y] = (*BODY, 255)
    for x in (4, 5, 10, 11):
        for y in (5, 6):
            pixels[ox + x, y] = (*EYE, 255)


def main() -> None:
    atlas = Image.open(ATLAS).convert('RGBA')
    pixels = atlas.load()
    bake_bullet(pixels)
    bake_boss_placeholder(pixels)
    atlas.save(ATLAS)
    print(f'baked tiles {BOSS_TILE}, {BULLET_TILE} into {ATLAS}')


if __name__ == '__main__':
    main()
```

- [ ] **Step 3: 焼いて中身を確かめる**

```bash
uv run --with pillow python tools/boss_tiles.py
```

```bash
uv run --with pillow python -c "
from PIL import Image
im = Image.open('m/q2.png').convert('RGBA')
for idx in (45, 46):
    px = [p for p in im.crop((idx*16,0,(idx+1)*16,16)).get_flattened_data() if p[3] >= 204]
    fb = [p for p in px if p[0] > 242 and p[1] > 63 and p[2] == 0]
    print(idx, 'opaque', len(px), 'full-bright', len(fb))
"
```
Expected: `45 opaque 156 full-bright 8` / `46 opaque 32 full-bright 32`（弾は全ピクセルが full-bright であること、ボスは目だけが full-bright であることが要点。総数は多少ずれてよい）

- [ ] **Step 4: `tools/atlas.py` の範囲を伸ばす**

```python
TILE_RANGE = range(33, 47)
```

docstring の 1 行目も `"""m/q2.png のタイル 33〜46 に画像を焼き込む。` に直し、使い方の行を `<src_dir> に 33.png .. 46.png を置く` にする。43・44・46 はコードで作る帯と点なので元画像を置かなければ黙って飛ばされる、という注記を docstring の末尾に足す。

- [ ] **Step 5: `images.md` にプロンプトを書く**

リポジトリ直下に作る。既存アイコンと同じく日英併記で、16×16 に縮小されることを前提にシルエット重視の指示にする。

```markdown
# 画像生成プロンプト

アトラス `m/q2.png` のタイル 45（ボス = 灰皿撤去ユニット）1 枚ぶん。
生成した PNG を `45.png` という名前でディレクトリに置き、
`uv run --with pillow python tools/atlas.py <そのディレクトリ>` で焼き込む。
焼き込んだらこのファイルは削除する（役目を終えるため）。

## 共通の制約

- **正方形。16×16 ピクセルに縮小されて使われる。** ディテールではなくシルエットで読ませること
- **背景は単色べた塗り。** 左上 (0,0) のピクセル色が背景キーとして透過に落とされる。機体の色と明確に違う色にすること
- **目・発光部は純粋な橙赤 `#FF4200`。** この色だけがシェーダの full-bright 規則を通り、暗いフロアでも光って見える。他の部分にこの色を使わないこと
- 機体色は無彩色寄りの灰（既存のセントリー・清掃ドローンと同じ帯）
- 正面向き、上下は切らない。真横や俯瞰にしない（ビルボードとして常に正面を向く）

## タイル 45: 灰皿撤去ユニット

日本語: 巨大な業務用の解体ロボット。ずんぐりした箱型の胴体に、放射状に伸びる
複数の短い砲身。低い姿勢でうずくまり、何かの上に居座っているような重量感。
胴体中央に横一文字の橙赤のスリット（単眼）。傷と煤で汚れた灰色の装甲。
背景は濃い青緑の単色。

English: A massive industrial demolition robot, squat boxy chassis with several
short gun barrels radiating outward from its torso. Low crouching posture, heavy
and immovable, as if squatting on top of something. A single horizontal slit eye
in pure orange-red (#FF4200) across the center of the chassis. Grey armor plating,
scratched and soot-stained. Flat solid dark teal background. Front view, full body
in frame, pixel-art friendly silhouette.
```

- [ ] **Step 6: commit**

```bash
git add tools/boss_tiles.py tools/atlas.py m/q2.png images.md
git commit -m "ボスの弾と暫定の機体をアトラス 45・46 に焼き込む"
```

---

### Task 4: ボスの数値と掃射の幾何（`boss-model.ts`）

3D の見た目は自動で確認できないので、形の性質だけをモデル側で固定する（`slash-model.ts` と同じ流儀）。

**Files:**
- Create: `source/boss-model.ts`
- Test: `source/boss-model.test.ts`

**Interfaces:**
- Consumes: なし（実行時 import を一切持たない）
- Produces:
  - `boss_arms(depth: number): number`
  - `boss_hp(depth: number): number`
  - `boss_volleys(before: number, after: number): number`
  - `boss_arm_angles(angle: number, arms: number): number[]`
  - 定数 `boss_arms_max`, `boss_spin_rate`, `boss_fire_step`, `boss_bullet_speed`

- [ ] **Step 1: 失敗するテストを書く**

`source/boss-model.test.ts`

```ts
import { describe, expect, it } from 'vitest'
import {
  boss_arm_angles, boss_arms, boss_arms_max, boss_fire_step, boss_hp, boss_volleys,
} from './boss-model'

describe('boss_arms', () => {
  it('5 階ごとに 1 本増え、6 本で頭打ちになる', () => {
    expect(boss_arms(5)).toBe(2)
    expect(boss_arms(10)).toBe(3)
    expect(boss_arms(15)).toBe(4)
    expect(boss_arms(20)).toBe(5)
    expect(boss_arms(25)).toBe(6)
    expect(boss_arms(100)).toBe(boss_arms_max)
  })

  it('深度に対して単調非減少である', () => {
    for (let depth = 5; depth <= 200; depth += 5) {
      expect(boss_arms(depth)).toBeGreaterThanOrEqual(boss_arms(depth - 5))
    }
  })
})

describe('boss_hp', () => {
  it('深度 5 で 60 発、以降 5 階ごとに 20 発増える', () => {
    expect(boss_hp(5)).toBe(60)
    expect(boss_hp(10)).toBe(80)
    expect(boss_hp(20)).toBe(120)
  })

  it('深度に対して狭義単調増加である', () => {
    for (let depth = 5; depth <= 200; depth += 5) {
      expect(boss_hp(depth)).toBeGreaterThan(boss_hp(depth - 5))
    }
  })
})

describe('boss_volleys', () => {
  it('しきい値をまたいだ回数だけ斉射する', () => {
    expect(boss_volleys(0, boss_fire_step * 0.9)).toBe(0)
    expect(boss_volleys(0, boss_fire_step * 1.1)).toBe(1)
    expect(boss_volleys(boss_fire_step * 0.9, boss_fire_step * 1.1)).toBe(1)
    expect(boss_volleys(0, boss_fire_step * 2.1)).toBe(2)
  })

  it('掃引を細かく刻んでも合計の斉射数は変わらない', () => {
    const total = boss_fire_step * 10
    let coarse = boss_volleys(0, total)
    let fine = 0
    for (let i = 0; i < 1000; i++) {
      fine += boss_volleys(total * i / 1000, total * (i + 1) / 1000)
    }
    expect(fine).toBe(coarse)
    expect(coarse).toBe(10)
  })
})

describe('boss_arm_angles', () => {
  it('本数ぶんの角度を等角に返す', () => {
    const angles = boss_arm_angles(0, 4)
    expect(angles.length).toBe(4)
    for (let i = 1; i < angles.length; i++) {
      expect(angles[i] - angles[i - 1]).toBeCloseTo(Math.PI / 2)
    }
  })

  it('第 1 砲口は掃引の角度そのものを向く', () => {
    expect(boss_arm_angles(1.23, 3)[0]).toBe(1.23)
  })
})
```

- [ ] **Step 2: 失敗を確認する**

Run: `npm test -- boss-model`
Expected: FAIL。`Failed to resolve import "./boss-model"`

- [ ] **Step 3: `source/boss-model.ts` を書く**

```ts
// ボスの数値と掃射の幾何。実行時 import を一切持たない葉モジュール。
// 3D の見た目は自動で確認できないため、形の性質だけをここで固定する
// （slash-model.ts と同じ流儀）。

// 砲口の本数の上限。O(n²) の衝突ループに乗る弾の数を抑える意味もある
export const boss_arms_max = 6

// 砲口の本数。深度スケールはこの 1 本だけで表す。見た目で強さが分かり、
// 無限深度でも上限で頭打ちになる。回転速度と弾速を一緒に動かさないのは、
// 二重にスケールすると難度が跳ねるため
export function boss_arms(depth: number): number {
  return Math.min(1 + Math.floor(depth / 5), boss_arms_max)
}

// 耐久（自機のプラズマ 1 発 = 1 ダメージ）。深度 5 の 60 発は、素の射撃間隔
// 0.1 秒なら当てっぱなしで 6 秒、回避しながらで 20〜40 秒。同深度のゲージ
// 寿命は約 72 秒なので「倒せるが、のんびりはできない」帯に収まる
export function boss_hp(depth: number): number {
  return 40 + 4 * depth
}

// 砲塔の角速度（rad/s）。1 周 12.6 秒
export const boss_spin_rate = 0.5

// 発射を刻む角度（rad）。時間ではなく掃引した角度で刻むのは、回転速度を
// 変えても弾の空間密度が変わらないようにするため
export const boss_fire_step = 0.18

// 弾速。セントリーの 64 より遅い。中央から闘技場の端（88px）まで約 1.6 秒
export const boss_bullet_speed = 56

// 掃引が発射のしきい値を何回またいだか。またいだ回数だけ斉射する。
// 引数は累積の掃引角（常に増える）で、回転の向きは含まない
export function boss_volleys(before: number, after: number): number {
  return Math.floor(after / boss_fire_step) - Math.floor(before / boss_fire_step)
}

// n 本の砲口の角度。等角に並んだまま全体が回る
export function boss_arm_angles(angle: number, arms: number): number[] {
  const out: number[] = []
  for (let i = 0; i < arms; i++) {
    out.push(angle + (i * Math.PI * 2) / arms)
  }
  return out
}
```

- [ ] **Step 4: テストを通す**

Run: `npm test -- boss-model && npm run typecheck`
Expected: 全 PASS

- [ ] **Step 5: commit**

```bash
git add source/boss-model.ts source/boss-model.test.ts
git commit -m "ボスの数値と掃射の幾何を boss-model.ts に置く"
```

---

### Task 5: 闘技場の生成

**Files:**
- Modify: `source/level-generator.ts`
- Test: `source/level-generator.test.ts`

**Interfaces:**
- Consumes: なし
- Produces:
  - `level_layout_t.boss: tile_pos_t | null`
  - `level_layout_t.boss_spin: number`（`1` / `-1`。ボス階以外は `0`）
  - `is_boss_depth(depth: number): boolean`
  - `arena_side: number`（テストと docs が読む）

- [ ] **Step 1: 失敗するテストを書く**

`source/level-generator.test.ts` の末尾に追加する。import 行に `arena_side`, `is_boss_depth` を足すこと。ヘルパー `reachable_from` / `is_floor` / `tile_index` はファイル先頭の既存のものを使う。

```ts
describe('ボス階の闘技場', () => {
  it('5 の倍数だけがボス階である', () => {
    expect(is_boss_depth(5)).toBe(true)
    expect(is_boss_depth(10)).toBe(true)
    expect(is_boss_depth(4)).toBe(false)
    expect(is_boss_depth(6)).toBe(false)
  })

  it('ボスと本物の喫煙所が盤面中央の同じタイルに立つ', () => {
    const layout = generate_level(5, 12345)
    expect(layout.boss).toEqual({ x: level_width >> 1, z: level_height >> 1 })
    expect(layout.smoking_area).toEqual(layout.boss)
  })

  it('通常フロアには boss が無い', () => {
    const layout = generate_level(6, 12345)
    expect(layout.boss).toBe(null)
    expect(layout.boss_spin).toBe(0)
  })

  it('雑魚・ダミー・床のヤニ・ドローンを置かない', () => {
    for (let seed = 1; seed <= 200; seed++) {
      const layout = generate_level(5, seed)
      expect(layout.spiders.length).toBe(0)
      expect(layout.sentries.length).toBe(0)
      expect(layout.dummies.length).toBe(0)
      expect(layout.yani.length).toBe(0)
      expect(layout.drones.length).toBe(0)
      expect(layout.health.length).toBeGreaterThanOrEqual(2)
      expect(layout.health.length).toBeLessThanOrEqual(4)
    }
  })

  it('開始地点と非常口が対角の隅にある', () => {
    for (let seed = 1; seed <= 200; seed++) {
      const layout = generate_level(10, seed)
      expect(Math.abs(layout.start.x - layout.exit.x)).toBe(arena_side - 5)
      expect(Math.abs(layout.start.z - layout.exit.z)).toBe(arena_side - 5)
    }
  })

  it('回転の向きは ±1 で、シードによって両方が出る', () => {
    const spins = new Set<number>()
    for (let seed = 1; seed <= 200; seed++) {
      spins.add(generate_level(5, seed).boss_spin)
    }
    expect([...spins].sort()).toEqual([-1, 1])
  })

  it('1000 シードで全床タイルが開始地点から到達できる', () => {
    for (let seed = 1; seed <= 1000; seed++) {
      const layout = generate_level(5, seed)
      const seen = reachable_from(layout)
      for (let i = 0; i < layout.tiles.length; i++) {
        const x = i % level_width
        const z = (i / level_width) | 0
        if (is_floor(layout.tiles, x, z)) { expect(seen[i]).toBe(1) }
      }
    }
  })

  it('柱は中央の灰皿にも外周の輪郭壁にも接しない', () => {
    const layout = generate_level(5, 777)
    const cx = level_width >> 1
    const cz = level_height >> 1
    const half = arena_side >> 1
    for (let z = cz - half; z <= cz + half; z++) {
      for (let x = cx - half; x <= cx + half; x++) {
        // 闘技場の内側で床でないのは、中央の灰皿・隅の非常口・柱だけ。
        // 前の 2 つを除いた残りが柱である
        if (is_floor(layout.tiles, x, z)) { continue }
        if (x === cx && z === cz) { continue }
        if (x === layout.exit.x && z === layout.exit.z) { continue }
        // 柱のタイル: 中央からも外周からも 1 タイル以上離れていること
        expect(Math.max(Math.abs(x - cx), Math.abs(z - cz))).toBeGreaterThan(1)
        expect(Math.abs(x - cx)).toBeLessThan(half - 1)
        expect(Math.abs(z - cz)).toBeLessThan(half - 1)
      }
    }
  })

  it('頂点コストが上限に収まる', () => {
    for (let seed = 1; seed <= 200; seed++) {
      expect(level_vert_cost(generate_level(5, seed).tiles)).toBeLessThan(60000)
    }
  })
})
```

- [ ] **Step 2: 失敗を確認する**

Run: `npm test -- level-generator`
Expected: FAIL。`arena_side` と `is_boss_depth` が `./level-generator` に存在しない

- [ ] **Step 3: `level_layout_t` を広げ、`take` を共通化する**

`source/level-generator.ts` の `level_layout_t` に 2 行足す。

```ts
  drones: tile_pos_t[] // 清掃ドローン。0〜1 体
  boss: tile_pos_t | null // ボス。ボス階（深度が 5 の倍数）以外は null
  boss_spin: number // 砲塔の回転方向（1 / -1）。ボス階以外は 0
}
```

`build_layout` の中に定義されている `take` クロージャをモジュールレベルの関数に上げ、`build_layout` からも `build_arena` からも呼べるようにする。`build_layout` 側は `take(n)` の呼び出しを `take_from(spawnable, n)` に書き換え、クロージャの定義を削除する。

```ts
// 候補から重複なく取り出す。候補が尽きたら取れた分で打ち切るので、
// 呼ぶ順序がそのまま優先度になる（docs/enemies.md「総数の内訳」）
function take_from(spawnable: number[], count: number): tile_pos_t[] {
  const out: tile_pos_t[] = []
  for (let i = 0; i < count && spawnable.length > 0; i++) {
    const pick = random_int(0, spawnable.length - 1)
    const index = spawnable[pick]
    spawnable.splice(pick, 1)
    out.push({ x: index % level_width, z: (index / level_width) | 0 })
  }
  return out
}
```

`build_layout` の `return` に 2 フィールドを足す。

```ts
  return {
    tiles, rooms, start, smoking_area, dummies, exit,
    spiders, sentries, health, yani, drones,
    boss: null, boss_spin: 0,
  }
```

- [ ] **Step 4: `build_arena()` を書く**

`generate_level` の直前に置く。定数は `depth_scale` の近く（フロアの広さに関する定数群）に置く。

```ts
// ボス階（深度が 5 の倍数）の闘技場。一辺は深度で変えない — レンダラの霧が
// smoothstep(112, 16, 深度) で切れるので、中央から端まで 88px（11 タイル）なら
// どこに立ってもボスが見える。闘技場は相手が見えていないと成立しない。
// 広さを深度で動かすと、掃射の間合いが深度ごとに別物になる
export const arena_side = 23
// 柱は中心から半径 6 タイルのリングに 45° ごと 8 本、1 本 2×2。格子状に
// しないのは、中央のボスから見て全方向に等しく遮蔽が要るため（格子だと
// 隅が空いて安全地帯が偏り、周回が 360° 成立しない）
const arena_pillar_radius = 6
const arena_pillar_count = 8
const arena_pillar_size = 2

export function is_boss_depth(depth: number): boolean {
  return depth % 5 === 0
}

function build_arena(seed: number): level_layout_t {
  random_seed(seed)
  for (let i = 0; i < rng_warmup; i++) { random_int(0, 1) }

  const tiles = new Uint8Array(level_width * level_height)
  const cx = level_width >> 1
  const cz = level_height >> 1
  const half = arena_side >> 1
  const room: room_t = { x: cx - half, z: cz - half, w: arena_side, h: arena_side }
  carve_room(tiles, room)

  // 柱は掘ったあとに床を消し、build_walls() に壁として立ててもらう。
  // 2×2 は偶数辺なので中心タイルに対する厳密な鏡像は取れない。正方向は
  // 中心 + r、負方向は中心 - r - 1 に頭を置くと、両側が中心をまたいで対称になる
  for (let i = 0; i < arena_pillar_count; i++) {
    const angle = (i * Math.PI * 2) / arena_pillar_count
    const dx = Math.round(Math.cos(angle) * arena_pillar_radius)
    const dz = Math.round(Math.sin(angle) * arena_pillar_radius)
    const px = cx + (dx < 0 ? dx - arena_pillar_size + 1 : dx)
    const pz = cz + (dz < 0 ? dz - arena_pillar_size + 1 : dz)
    for (let z = 0; z < arena_pillar_size; z++) {
      for (let x = 0; x < arena_pillar_size; x++) {
        tiles[tile_index(px + x, pz + z)] = 0
      }
    }
  }

  build_walls(tiles)

  // 隅は外周の輪郭壁から 2 タイル内側。順序を時計回りにしてあるので、
  // +2 で必ず対角になる
  const corners: tile_pos_t[] = [
    { x: cx - half + 2, z: cz - half + 2 },
    { x: cx + half - 2, z: cz - half + 2 },
    { x: cx + half - 2, z: cz + half - 2 },
    { x: cx - half + 2, z: cz + half - 2 },
  ]
  const start_corner = random_int(0, 3)
  const start = corners[start_corner]
  const exit = corners[(start_corner + 2) % 4]
  const smoking_area: tile_pos_t = { x: cx, z: cz }
  // 向きをシードで決める。固定すると間取りの暗記と同じことが起きる
  // （docs/gameplay.md「シード方針」）
  const boss_spin = random_int(0, 1) ? 1 : -1

  // 喫煙所と非常口はブロックとして立つので当たり判定を壁にする
  // （build_layout と同じ理由。見た目はエンティティが毎フレーム描く）
  for (const p of [smoking_area, exit]) {
    tiles[tile_index(p.x, p.z)] = 8
  }

  const dist = bfs_distances(tiles, start)
  const spawnable: number[] = []
  for (let i = 0; i < dist.length; i++) {
    const t = tiles[i]
    if (t > 0 && t < 8 && dist[i] >= spawn_min_distance) { spawnable.push(i) }
  }

  // ヘルスパックだけ置く。柱の陰に散るので、掃射の合間に取りに動くかが
  // 判断になる。雑魚・ダミー・床のヤニ・清掃ドローンを置かない理由は
  // docs/gameplay.md「ボス階」にある
  return {
    tiles, rooms: [room], start, smoking_area, dummies: [], exit,
    spiders: [], sentries: [], health: take_from(spawnable, random_int(2, 4)),
    yani: [], drones: [],
    boss: smoking_area, boss_spin,
  }
}
```

- [ ] **Step 5: `generate_level` を分岐させる**

```ts
export function generate_level(depth: number, seed: number): level_layout_t {
  // 闘技場は形が決まっているので、間取りの棄却（layout_attempts）も
  // 頂点コストの検査も要らない。床 497 + 壁 128 タイルで約 6000 頂点しかない
  if (is_boss_depth(depth)) { return build_arena(seed) }

  for (let attempt = 0; attempt < layout_attempts; attempt++) {
```

- [ ] **Step 6: テストを通す**

Run: `npm test -- level-generator && npm run typecheck`
Expected: 全 PASS。既存のテストが深度 5・10 を通常フロアとして検査していたら、その `it` を非ボス階の深度（4・6・11 など）に読み替える。棄却する前に必ず「その検査がボス階にも当てはまるべきか」を確認すること

- [ ] **Step 7: commit**

```bash
git add source/level-generator.ts source/level-generator.test.ts
git commit -m "深度が 5 の倍数のフロアを 1 部屋の闘技場にする"
```

---

### Task 6: ボスのエンティティと弾

**Files:**
- Create: `source/entity-boss.ts`
- Modify: `source/state.ts`（`boss_alive`）
- Modify: `source/game.ts`（`load_level` で生成、`run_start` でリセット）
- Test: `source/game.test.ts`

**Interfaces:**
- Consumes: `boss_arms` / `boss_hp` / `boss_volleys` / `boss_arm_angles` / `boss_spin_rate` / `boss_bullet_speed`（Task 4）、`layout.boss` / `layout.boss_spin`（Task 5）、`entity_t.w`（Task 1）
- Produces:
  - `entity_boss_t`（フィールド `_spin: number` / `_arms: number` を生成側が代入する）
  - `entity_boss_plasma_t`（メソッド `_expire(): void`）
  - `state.boss_alive: number`

- [ ] **Step 1: 失敗するテストを書く**

`source/game.test.ts` に追加する。import に `entity_boss_t, entity_boss_plasma_t` を足すこと。

```ts
// 深度 5 まで降りる。非常口の開通を待たずに次のフロアへ行けるよう、
// 降下は state を直接触らずに game.ts の予約経路を使う
function descend_to(depth: number): void {
  while (state.depth < depth) {
    state.exit_open = 1
    state.descend_timer = 0.01
    advance(0.125)
  }
}

describe('ボス', () => {
  beforeEach(() => { start_run() })

  it('ボス階でだけ湧き、耐久が深度で決まる', () => {
    descend_to(5)
    const bosses = state.entities.filter((e) => e instanceof entity_boss_t)
    expect(bosses.length).toBe(1)
    expect(bosses[0].h).toBe(60)
    expect(state.boss_alive).toBe(1)
  })

  it('通常フロアには湧かない', () => {
    descend_to(4)
    expect(state.entities.some((e) => e instanceof entity_boss_t)).toBe(false)
    expect(state.boss_alive).toBe(0)
  })

  // 何発が何度から出るかは boss-model.test.ts が固定する。ここで見るのは
  // 「弾が生まれて生き残る」配線だけ — 銃口が灰皿タイルの中にあると、
  // 生まれた次のフレームで壁判定に消される
  it('掃射で弾を吐き、その弾が壁で即死しない', () => {
    descend_to(5)
    advance(1)
    expect(state.entities.some((e) => e instanceof entity_boss_plasma_t)).toBe(true)
  })

  it('動かない', () => {
    descend_to(5)
    const boss = state.entities.find((e) => e instanceof entity_boss_t)!
    const { x, z } = boss
    advance(2)
    expect(boss.x).toBe(x)
    expect(boss.z).toBe(z)
  })
})
```

- [ ] **Step 2: 失敗を確認する**

Run: `npm test -- game.test.ts`
Expected: FAIL。`Failed to resolve import "./entity-boss"`

- [ ] **Step 3: `state.ts` にフラグを足す**

`exit_open` の直後に置く。

```ts
  // ボスが生きている間は 1。灰皿に居座っているので一服できない
  // （entity-smoking-area.ts のガード）。load_level が立て、
  // entity-boss の _kill() が下ろす
  boss_alive: 0,
```

- [ ] **Step 4: `source/entity-boss.ts` を書く**

この時点では撃破処理は最小（`state.boss_alive` を下ろすだけ）にする。演出・ドロップ・報酬は Task 8 と Task 11 で足す。

```ts
import {
  boss_arm_angles, boss_bullet_speed, boss_hp, boss_spin_rate, boss_volleys,
} from './boss-model'
import { entity_t } from './entity'
import { spawn_particles } from './entity-particle'
import { entity_player_t } from './entity-player'
import { camera, push_light, push_quad } from './renderer'
import { state } from './state'

// 灰皿撤去ユニット。深度が 5 の倍数のフロアで、中央の灰皿の上に居座る。
// 倒すまで一服できない（entity-smoking-area.ts が state.boss_alive を見る）。
//
// 砲口が等角に並んだまま全体が回り、掃引が一定角度進むごとに全砲口から
// 1 発ずつ吐く。回転しているので柱の陰（安全地帯）も一緒に回り、自機は
// ボスの周りを同じ向きに回り続けることになる。回りながら撃ち返す形は
// docs/gameplay.md「操作」の後退射撃そのもので、膠着が構造的に起きない。

// 見た目の一辺（ワールド単位）。通常スプライトの 2 倍。push_sprite() は
// 6 固定で拡大できないので、entity-slash.ts と同じく push_quad() を直に呼ぶ
const boss_size = 12
// 灰皿ブロック（高さ 8）の上に立たせる。全高 20 になり、「灰皿に座り込んだ
// 大型機」が一目で読める。衝突判定は x/z だけなので、この高さは見た目専用
const boss_base_y = 8
// 当たり判定の一辺。見た目だけ大きくして既定の 9 のまま残すと、輪郭に
// 撃った弾がすり抜ける
const boss_hitbox = 14
// 銃口の半径。灰皿タイル（8×8）の中心から測る。中から出すと、生まれた次の
// フレームで _collides() が壁を返して弾が即座に消える。弾の判定は 6×4 なので、
// タイルから抜けるには x で 7・z で 6 の余裕が要る。半径 10 なら最悪の角度
// （斜め 45°）でも成分が 7.07 になり、必ずどちらかの軸で抜ける
const boss_muzzle = 10
const boss_bullet_tile = 46

export class entity_boss_t extends entity_t {
  // 生成側が代入する — _init() には基底クラスのフィールドしか書けない
  // （entity-container.ts の _slot と同じ理由）
  _spin = 1
  _arms = 2

  // 掃引した総角度。常に増える。発射はこれで刻むので、回転の向きを含めない
  private _swept = 0
  private _angle = 0

  protected override _init(): void {
    this.h = boss_hp(state.depth)
    this.w = boss_hitbox
  }

  override _update(): void {
    const t = this
    const swept_before = t._swept
    t._swept += boss_spin_rate * state.time_elapsed
    t._angle = t._swept * t._spin

    const volleys = boss_volleys(swept_before, t._swept)
    for (let v = 0; v < volleys; v++) {
      for (const angle of boss_arm_angles(t._angle, t._arms)) {
        // 灰皿タイルの中心（t.x + 4）から銃口の半径だけ離して出す。
        // 弾の判定は 6×4 なので、その中心を銃口に合わせる
        const mx = t.x + 4 + Math.cos(angle) * boss_muzzle
        const mz = t.z + 4 + Math.sin(angle) * boss_muzzle
        new entity_boss_plasma_t(mx - 3, boss_base_y, mz - 2, 0, boss_bullet_tile, angle)
      }
    }

    // 基底の _update() は呼ばない。動かないので積分が要らないうえ、
    // 灰皿タイルは壁なので毎フレーム _collides() が真になる
  }

  override _render(): void {
    const t = this
    // push_sprite() の中身を、6 ではなく boss_size で組み直したもの
    const tilt = 3 + (camera.z + t.z) / 12
    const x = t.x - (boss_size - 6) / 2
    const y = boss_base_y
    push_quad(
      x, y + boss_size, t.z,
      x + boss_size, y + boss_size, t.z,
      x, y, t.z + tilt,
      x + boss_size, y, t.z + tilt,
      0, 0, 1, t.s,
    )
    // ライトを持つのは本体だけ。弾は full-bright で描くので光を要らない
    push_light(t.x + 3, y + 6, t.z + 6, 1.2, 0.4, 0.2, 0.05)
  }

  override _receive_damage(from: entity_t, amount: number): void {
    super._receive_damage(from, amount)
    // ノックバックを受けない。据え置きの砲台であることが被弾の反応で読める
    // （docs/enemies.md「被弾のノックバックは硬さの表現である」）
    spawn_particles(this, 3)
  }

  override _check(other: entity_t): void {
    // 灰皿に近づけない。生きている間は接触そのものが壁になる
    if (other instanceof entity_player_t) {
      other._receive_damage(this, 1)
    }
  }

  protected override _kill(): void {
    if (this._dead) { return } // 二重加算を防ぐ
    super._kill()
    state.kills++
    state.boss_alive = 0
  }
}

export class entity_boss_plasma_t extends entity_t {
  protected override _init(angle: number): void {
    this.vx = Math.cos(angle) * boss_bullet_speed
    this.vz = Math.sin(angle) * boss_bullet_speed
  }

  // ライトを積まない。同時に最大 26 発飛ぶので max_lights = 16 を超え、
  // 光る弾と光らない弾が混ざる。代わりにタイル 46 を full-bright 規則
  // （r>0.95 && g>0.25 && b==0）を満たす色で焼いてあり、ライトも霧も通さず
  // 全弾が等しく明るく見える（tools/boss_tiles.py）。弾幕は全部見えることが
  // 要件なので、これは妥協ではなく正しい経路

  // 撃破時にボスがまとめて消すための入口。_kill() は protected なので
  // 兄弟クラスからは呼べない
  _expire(): void {
    this._kill()
  }

  protected override _did_collide(): void {
    this._kill()
  }

  override _check(other: entity_t): void {
    if (other instanceof entity_player_t) {
      other._receive_damage(this, 1)
      this._kill()
    }
  }
}
```

- [ ] **Step 5: `game.ts` から生成する**

import に足す。

```ts
import { boss_arms } from './boss-model'
import { entity_boss_t } from './entity-boss'
```

`run_start()` の `state.melee_active = 0` の隣に足す。

```ts
  state.boss_alive = 0
```

`load_level()` の `state.smoking = 0` の隣に足す。

```ts
  state.boss_alive = 0
```

`load_level()` のエンティティ生成、`for (const p of layout.drones)` の直後に足す。

```ts
  // ボス階だけ。灰皿と同じタイルに立つので、生きている間は自機が
  // 喫煙所に触れられない
  if (layout.boss) {
    const boss = new entity_boss_t(layout.boss.x * 8, 0, layout.boss.z * 8, 0, 45)
    boss._spin = layout.boss_spin
    boss._arms = boss_arms(depth)
    state.boss_alive = 1
  }
```

- [ ] **Step 6: テストを通す**

Run: `npm test && npm run typecheck`
Expected: 全 PASS

- [ ] **Step 7: commit**

```bash
git add source/entity-boss.ts source/state.ts source/game.ts source/game.test.ts
git commit -m "灰皿に居座って回転掃射するボスを置く"
```

---

### Task 7: 既存の攻撃を通し、一服を塞ぐ

`entity-plasma.ts` も薙ぎも対象を 3 クラスで名指し列挙しているため、足さないとボスに一切ダメージが入らない。

**Files:**
- Modify: `source/entity-plasma.ts`
- Modify: `source/entity-player.ts`（`_swing`）
- Modify: `source/entity-smoking-area.ts`
- Test: `source/game.test.ts`

**Interfaces:**
- Consumes: `entity_boss_t`（Task 6）、`state.boss_alive`（Task 6）
- Produces: なし

- [ ] **Step 1: 失敗するテストを書く**

`source/game.test.ts` の `describe('ボス')` に追加する。

```ts
  it('自機のプラズマでダメージが入る', () => {
    descend_to(5)
    const boss = state.entities.find((e) => e instanceof entity_boss_t)!
    const before = boss.h
    // 判定（幅 14）の中に置いて止める。灰皿タイルは壁なので、動かすと
    // 当たる前に _did_collide() で消える。見たいのは配線だけなので静止させる
    const shot = new entity_plasma_t(boss.x + 10, 0, boss.z, 0, 26, 0)
    shot.vx = 0
    shot.vz = 0
    step()
    expect(boss.h).toBe(before - 1)
  })

  it('ボスが生きている間は一服が始まらない', () => {
    descend_to(5)
    const boss = state.entities.find((e) => e instanceof entity_boss_t)!
    const player = state.entity_player!
    // 灰皿の真横に立たせる（喫煙所エンティティに接触する位置）
    player.x = boss.x - 8
    player.z = boss.z
    advance(0.5)
    expect(state.smoking).toBe(0)
    expect(state.exit_open).toBe(0)
  })
```

- [ ] **Step 2: 失敗を確認する**

Run: `npm test -- game.test.ts`
Expected: FAIL。1 本目はダメージが入らず `boss.h` が変わらない

- [ ] **Step 3: プラズマの対象に足す**

`source/entity-plasma.ts` の import に `import { entity_boss_t } from './entity-boss'` を足し、`_check` の条件に 1 行加える。

```ts
    if (
      other instanceof entity_spider_t ||
      other instanceof entity_sentry_t ||
      other instanceof entity_drone_t ||
      other instanceof entity_boss_t
    ) {
```

（`entity-*.ts` どうしの循環は許容されている。壊れるのは `entity.ts` からサブクラスへ到達したときだけ。`docs/architecture.md`「循環参照の不変条件」）

- [ ] **Step 4: 薙ぎの対象に足す（一撃必殺は除外）**

`source/entity-player.ts` の import に `entity_boss_t` を足し、`_swing()` の対象判定を書き換える。

```ts
      const sentry = e instanceof entity_sentry_t
      const boss = e instanceof entity_boss_t
      if (!spider && !drone && !sentry && !boss) { continue }
```

`kills` の式は変えない。ボスがどの項にも現れないことがそのまま「一撃必殺が効かない」になるので、意図をコメントで固定する。

```ts
      // ボスはどの項にも現れない。刃物 Lv9 以上の「硬さを無視して一撃で
      // 落とす」がボスに効くと、耐久で作った戦いが丸ごと消える。通常の
      // blade_damage() は通すので、刃物ビルドが締め出されることはない
      const kills =
        spider ||
        (drone && oneshot >= blade_oneshot_drone) ||
        (sentry && oneshot >= blade_oneshot_all)
```

- [ ] **Step 5: 一服を塞ぐ**

`source/entity-smoking-area.ts` の `_render()` にある一服の条件に 1 項足す。

```ts
    // state.boss_alive: ボス階の灰皿にはボスが居座っている。倒すまで
    // 吸わせない（docs/gameplay.md「ボス階」）
    if (touching && !this._done && !this._needs_release &&
        state.game_running && !state.dying && !state.boss_alive) {
```

- [ ] **Step 6: テストを通す**

Run: `npm test && npm run typecheck`
Expected: 全 PASS

- [ ] **Step 7: commit**

```bash
git add source/entity-plasma.ts source/entity-player.ts source/entity-smoking-area.ts source/game.test.ts
git commit -m "ボスに弾と薙ぎを通し、倒すまで一服できないようにする"
```

---

### Task 8: 撃破の演出とコンテナ 100%

**Files:**
- Modify: `source/entity-boss.ts`（`_kill`）
- Test: `source/game.test.ts`

**Interfaces:**
- Consumes: `entity_boss_plasma_t._expire()`（Task 6）
- Produces: なし

- [ ] **Step 1: 失敗するテストを書く**

`source/game.test.ts` の `describe('ボス')` に追加する。import に `entity_container_t` を足すこと。

```ts
  it('撃破で残弾が消え、コンテナが必ず 1 個落ちる', () => {
    descend_to(5)
    const boss = state.entities.find((e) => e instanceof entity_boss_t)!
    advance(1) // 弾を出させる
    expect(state.entities.some((e) => e instanceof entity_boss_plasma_t)).toBe(true)

    boss._receive_damage(state.entity_player!, 999)
    step()
    expect(state.boss_alive).toBe(0)
    expect(state.entities.some((e) => e instanceof entity_boss_plasma_t)).toBe(false)
    expect(state.entities.filter((e) => e instanceof entity_container_t).length).toBe(1)
  })
```

- [ ] **Step 2: 失敗を確認する**

Run: `npm test -- game.test.ts`
Expected: FAIL。弾が残り、コンテナが 0 個

- [ ] **Step 3: `_kill()` を書き足す**

`source/entity-boss.ts` の import に足す。

```ts
import { audio_play, audio_sfx_explode } from './audio'
import { entity_container_t } from './entity-container'
import { entity_explosion_t } from './entity-explosion'
import { gear_roll_slot, gear_roll_tier } from './equipment'
```

ファイル末尾に置く。

```ts
// 押収品コンテナを 1 個落とす。段は 2 回引いて高いほうを採る（best-of-2）。
// 段を底上げする専用テーブルを持たせると「段の抽選は深度で重み付けられる」
// （docs/equipment.md）が壊れる。best-of-2 なら重み付けの形はそのままで
// 期待値だけ上がるので、深く潜るほど良い装備という関係が保たれる
function drop_container(x: number, z: number): void {
  const container = new entity_container_t(x, 0, z, 5, 42)
  container._slot = gear_roll_slot(Math.random())
  container._tier = Math.max(
    gear_roll_tier(state.depth, Math.random()),
    gear_roll_tier(state.depth, Math.random()),
  )
}
```

`_kill()` を差し替える。

```ts
  protected override _kill(): void {
    if (this._dead) { return } // 二重加算を防ぐ
    super._kill()
    state.kills++
    state.boss_alive = 0

    // 残弾を消す。消さないと、勝った直後に流れ弾で一服が中断され
    // （docs/gameplay.md「一服」）、勝利の実感が濁る
    for (const e of state.entities) {
      if (e instanceof entity_boss_plasma_t) { e._expire() }
    }

    // 撃破演出はラン中で最も大きくする。序列は 蜘蛛 1 < セントリー 3 <
    // 銘品 4 < 自機の死 5 < 清掃ドローン 6 < ボス 8 で、docs/enemies.md の
    // 1 本の尺度に載せる。爆発を 1 点に重ねると光が固まって「割れた」感が
    // 出ないので、機体の周囲にずらして 5 発置く（ドローンの 3 発と同じ理屈）
    for (const [dx, dz] of [[0, 0], [7, 4], [-7, -4], [0, 9], [0, -9]]) {
      new entity_explosion_t(this.x + dx, 0, this.z + dz, 0, 26)
    }
    spawn_particles(this, 32)
    camera.shake = 8
    audio_play(audio_sfx_explode)

    drop_container(this.x, this.z)
  }
```

- [ ] **Step 4: テストを通す**

Run: `npm test && npm run typecheck`
Expected: 全 PASS

- [ ] **Step 5: commit**

```bash
git add source/entity-boss.ts source/game.test.ts
git commit -m "ボス撃破で残弾を消し、押収品コンテナを必ず落とす"
```

---

### Task 9: 強化 6 行の表示定義を `upgrade-rows.ts` に切り出す

同じ強化が死亡画面とボス報酬で別の書式になる余地を作らないため、行の定義を 1 か所にする。純粋な移動で表示は変わらない。

**Files:**
- Create: `source/upgrade-rows.ts`
- Modify: `source/death-screen.ts`

**Interfaces:**
- Consumes: なし
- Produces: `upgrade_row_t`（`id` / `name` / `icon` / `color` / `flavor` / `stat` / `value(level)`）と `upgrade_rows: upgrade_row_t[]`

- [ ] **Step 1: `source/upgrade-rows.ts` を作る**

`death-screen.ts` から次を**そのまま切り取って**移す。

- `interface upgrade_row_t { ... }`（コメント込み）→ `export interface`
- `const upgrade_rows: upgrade_row_t[] = [ ... ]`（`// 係数の百分率は Math.round を通す` のコメント込み）→ `export const`
- アイコン 6 枚の import（`icon_lung_url` / `icon_brain_url` / `icon_nose_url` / `icon_bullet_url` / `icon_cig_url` / `icon_leg_url`）
- `meta` からの `meta_nicotine_max` / `meta_drain_factor` / `meta_power_factor` / `meta_speed_factor` / `meta_spare_count` と型 `meta_upgrade_id_t`
- `nicotine` からの `nicotine_stage_normal` / `player_speed`

ファイル先頭に置くコメント:

```ts
// 恒久強化 6 行の表示定義。死亡画面（闇サイト）とボス撃破の報酬ダイアログの
// 両方が読む。同じ強化が 2 つの画面で別の書式になる余地を作らないため、
// 名前・色・フレーバー・効果の書式はここ 1 か所だけが持つ。
//
// 効果の数値は meta.ts の getter（段数を引数で受ける）から引き、式を
// 画面側に書き写さない（docs/meta-progression.md）。
```

- [ ] **Step 2: `death-screen.ts` から消して import に置き換える**

移した宣言と、そこでしか使われなくなった import を削除する。追加する行:

```ts
import { upgrade_rows } from './upgrade-rows'
```

`death-screen.ts` に残るのは `meta` / `meta_buy` / `meta_max_level` / `meta_upgrade_price` と、装備行や状態パネルが使う import だけになる。

- [ ] **Step 3: 未使用の import が残っていないか確かめる**

Run: `npm run typecheck`
Expected: PASS（`noUnusedLocals` が有効なら未使用の import はここで落ちる）

- [ ] **Step 4: 表示が変わっていないことを確かめる**

Run: `npm test && npm run build`
Expected: 全 PASS。ビルドが通ればアイコンの静的 import も解決している

- [ ] **Step 5: commit**

```bash
git add source/upgrade-rows.ts source/death-screen.ts
git commit -m "恒久強化の行定義を upgrade-rows.ts に切り出す"
```

---

### Task 10: 報酬の実効段と `meta_grant()`、`run_end()` での適用

**Files:**
- Create: `source/boss-reward-model.ts`
- Create: `source/boss-reward-model.test.ts`
- Modify: `source/meta.ts`
- Modify: `source/state.ts`
- Modify: `source/game.ts`
- Test: `source/meta.test.ts`, `source/game.test.ts`

**Interfaces:**
- Consumes: `meta_max_level` / `meta_upgrade_ids` / `meta_upgrade_id_t`
- Produces:
  - `reward_level(id, base, picked): number`
  - `reward_available(id, base, picked): boolean`
  - `reward_any_available(levels, picked): boolean`
  - `meta_grant(id: meta_upgrade_id_t): void`
  - `state.boss_levels: meta_upgrade_id_t[]`

- [ ] **Step 1: 失敗するテストを書く**

`source/boss-reward-model.test.ts`

```ts
import { describe, expect, it } from 'vitest'
import { reward_any_available, reward_available, reward_level } from './boss-reward-model'
import { meta_max_level } from './meta'
import type { meta_upgrade_id_t } from './meta'

const levels = (): Record<meta_upgrade_id_t, number> =>
  ({ lung: 0, tolerance: 0, sniff: 0, leg: 0, power: 0, spare: 0 })

describe('reward_level', () => {
  it('meta の段にこのランで選んだ回数を足す', () => {
    expect(reward_level('lung', 3, [])).toBe(3)
    expect(reward_level('lung', 3, ['lung'])).toBe(4)
    expect(reward_level('lung', 3, ['lung', 'leg', 'lung'])).toBe(5)
    expect(reward_level('lung', 3, ['leg'])).toBe(3)
  })

  it('上限を超えない', () => {
    expect(reward_level('sniff', 5, ['sniff'])).toBe(meta_max_level.sniff)
  })
})

describe('reward_available', () => {
  it('実効段が上限に達したら選べない', () => {
    expect(reward_available('sniff', 4, [])).toBe(true)
    expect(reward_available('sniff', 4, ['sniff'])).toBe(false)
    expect(reward_available('sniff', 5, [])).toBe(false)
  })
})

describe('reward_any_available', () => {
  it('1 本でも余地があれば true', () => {
    expect(reward_any_available(levels(), [])).toBe(true)
  })

  it('6 本すべてが上限なら false', () => {
    const maxed = { ...levels() }
    for (const id of Object.keys(maxed) as meta_upgrade_id_t[]) {
      maxed[id] = meta_max_level[id]
    }
    expect(reward_any_available(maxed, [])).toBe(false)
  })
})
```

`source/meta.test.ts` に追加する。

```ts
describe('meta_grant', () => {
  it('ヤニを引かずに 1 段上げる', () => {
    meta.yani = 100
    meta.levels.lung = 2
    meta_grant('lung')
    expect(meta.levels.lung).toBe(3)
    expect(meta.yani).toBe(100)
  })

  it('上限を超えない', () => {
    meta.levels.sniff = meta_max_level.sniff
    meta_grant('sniff')
    expect(meta.levels.sniff).toBe(meta_max_level.sniff)
  })
})
```

（`meta.test.ts` の既存の `beforeEach` が `meta` を初期化していればそれに乗る。無ければこの describe の中で使う値だけを明示的に代入すること）

`source/game.test.ts` の `describe('ボス')` に追加する。

```ts
  it('選んだ段はランの終わりに meta へ入る', () => {
    const before = meta.levels.leg
    state.boss_levels = ['leg', 'leg']
    kill_player()
    advance(3.125)
    expect(meta.levels.leg).toBe(before + 2)
  })
```

（import に `import { meta } from './meta'` を足すこと）

- [ ] **Step 2: 失敗を確認する**

Run: `npm test -- boss-reward-model meta.test game.test`
Expected: FAIL。`./boss-reward-model` が解決できず、`meta_grant` も存在しない

- [ ] **Step 3: `source/boss-reward-model.ts` を書く**

```ts
import { meta_max_level, meta_upgrade_ids } from './meta'
import type { meta_upgrade_id_t } from './meta'

// ボス報酬の実効段。報酬は run_end() まで meta に書かない（game.ts）ので、
// ダイアログが meta.levels をそのまま出すと、深度 5 と 10 で同じトラックを
// 選んだとき 2 回とも同じ「現在 → 次」が表示される。base は meta.levels[id]、
// picked はこのランで選んだ id の並び。

export function reward_level(
  id: meta_upgrade_id_t, base: number, picked: readonly meta_upgrade_id_t[],
): number {
  let level = base
  for (const p of picked) {
    if (p === id) { level++ }
  }
  return Math.min(level, meta_max_level[id])
}

export function reward_available(
  id: meta_upgrade_id_t, base: number, picked: readonly meta_upgrade_id_t[],
): boolean {
  return reward_level(id, base, picked) < meta_max_level[id]
}

// 6 本すべてが上限なら報酬ダイアログを出さない（呼び出し側はコンテナを
// もう 1 個落とす）。強化が満杯のプレイヤーに、まだ意味が残っている報酬は
// 装備の段だけである
export function reward_any_available(
  levels: Record<meta_upgrade_id_t, number>,
  picked: readonly meta_upgrade_id_t[],
): boolean {
  return meta_upgrade_ids.some((id) => reward_available(id, levels[id], picked))
}
```

- [ ] **Step 4: `meta.ts` に `meta_grant()` を足す**

`meta_buy()` の直後に置く。

```ts
// ボス撃破の報酬で 1 段上げる。meta_buy() と違ってヤニを引かない。
// 保存は呼び出し側（run_end）が最後に 1 度だけ行う — ボス報酬もヤニも
// 同じ 1 回の meta_save() に載る
export function meta_grant(id: meta_upgrade_id_t): void {
  meta.levels[id] = Math.min(meta.levels[id] + 1, meta_max_level[id])
}
```

- [ ] **Step 5: `state.ts` に積み場所を足す**

`yani_run` の直後に置く。ファイル冒頭の型 import に `import type { meta_upgrade_id_t } from './meta'` を足すこと（型のみなので「実行時 import を一切持たない」契約は破れない）。

```ts
  // ボス撃破で選んだ恒久強化。ヤニ（yani_run）と同じで、run_end() が meta へ
  // 合算するまで meta.levels は書かない。ラン中に直接書くと、毎フレーム
  // meta.levels を読む 4 本（耐性・脚力・火力・嗅覚）だけが先に効いて、
  // 6 本の効き方が不揃いになる
  boss_levels: [] as meta_upgrade_id_t[],
```

- [ ] **Step 6: `game.ts` で積み下ろす**

`run_start()` の `state.yani_run = 0` の隣に足す。

```ts
  state.boss_levels = []
```

`run_end()` の `meta.yani += state.yani_run` の直後に足す。import に `meta_grant` を加えること。

```ts
  // ボス撃破の報酬。寿命はヤニと同じで、途中でタブを閉じれば消える
  for (const id of state.boss_levels) { meta_grant(id) }
```

- [ ] **Step 7: テストを通す**

Run: `npm test && npm run typecheck`
Expected: 全 PASS

- [ ] **Step 8: commit**

```bash
git add source/boss-reward-model.ts source/boss-reward-model.test.ts source/meta.ts source/meta.test.ts source/state.ts source/game.ts source/game.test.ts
git commit -m "ボス報酬の段をヤニと同じ寿命で meta へ持ち越す"
```

---

### Task 11: 撃破報酬のダイアログ

**Files:**
- Create: `source/boss-reward.ts`
- Create: `source/boss-reward.css`
- Modify: `source/entity-boss.ts`（`_kill` から呼ぶ）
- Test: `source/game.test.ts`（モックの追加）

**Interfaces:**
- Consumes: `upgrade_rows`（Task 9）、`reward_level` / `reward_available` / `reward_any_available`（Task 10）、`state.paused`（Task 2）、`state.boss_levels`（Task 10）
- Produces: `boss_reward_show(): void`

- [ ] **Step 1: `source/boss-reward.css` を書く**

```css
/* ボス撃破の報酬ダイアログ。開封ダイアログ（#eq）と同じく、背後のフロアを
   隠しきらない中央パネルにする — 倒した相手が残骸として見えていることが、
   この報酬が何の対価なのかの答え合わせになる */
#br {
  position: fixed;
  inset: 0;
  display: none;
  place-items: center;
  background: rgba(0, 0, 0, 0.45);
  color: #b9dcc4;
  font-family: 'BIZ UDGothic', 'Yu Gothic UI', system-ui, sans-serif;
  z-index: 12;
  user-select: none;
}
#br .br-box {
  width: 34vw;
  padding: 1.4vw 1.6vw;
  border: 1px solid #f0932a;
  border-radius: 0.5vw;
  background: rgba(4, 10, 7, 0.94);
  box-shadow: 0 0 2vw #f0932a44;
  animation: brf 0.25s ease-out;
}
@keyframes brf { from { transform: scale(0.94); opacity: 0; } }
#br .br-head { font-size: 1.1vw; text-align: center; color: #f0932a; }
/* 効果はラン中には現れない。ここを読み落とすと報酬が壊れて見えるので、
   見出しの直下に必ず出す */
#br .br-sub { font-size: 0.75vw; text-align: center; opacity: 0.75; margin-bottom: 0.8vw; }
#br .br-row {
  display: grid;
  grid-template-columns: 2vw 1fr auto;
  align-items: center;
  gap: 0.6vw;
  padding: 0.35vw 0.5vw;
  border: 1px solid transparent;
  border-radius: 0.3vw;
}
#br .br-row.on { border-color: currentColor; background: rgba(255, 255, 255, 0.06); }
#br .br-row.maxed { opacity: 0.35; }
#br .br-row img { width: 2vw; }
#br .br-name { font-size: 0.9vw; }
#br .br-name small { display: block; font-size: 0.65vw; opacity: 0.6; color: #b9dcc4; }
#br .br-stat { font-size: 0.8vw; white-space: nowrap; }
#br .br-stat b { font-weight: 700; }
#br .br-keys { margin-top: 0.8vw; text-align: center; font-size: 0.7vw; opacity: 0.7; }
```

- [ ] **Step 2: `source/boss-reward.ts` を書く**

```ts
import { audio_play, audio_sfx_beep, audio_sfx_pickup } from './audio'
import { reward_available, reward_level } from './boss-reward-model'
import { key_spare, key_swap, keys } from './input'
import { meta, meta_max_level } from './meta'
import { state } from './state'
import { upgrade_rows } from './upgrade-rows'
import './boss-reward.css'

// ボス撃破の報酬。恒久強化 6 本から 1 本を 1 段上げる。
//
// Esc を持たない。「どれか必ず 1 段」なので辞退の経路を作らない。
// 選んだ段は state.boss_levels に積むだけで、meta へ入るのは run_end()。
// 効果がラン中に現れないことは見出しの直下に明記する。

let root: HTMLElement | null = null
let selected = 0

export function boss_reward_show(): void {
  state.paused = 1
  selected = first_available()

  if (!root) {
    root = document.createElement('div')
    root.id = 'br'
    document.body.appendChild(root)
  }
  render()
  root.style.display = 'grid'
  document.addEventListener('keydown', on_key)
  audio_play(audio_sfx_pickup)
}

function available(index: number): boolean {
  const id = upgrade_rows[index].id
  return reward_available(id, meta.levels[id], state.boss_levels)
}

function first_available(): number {
  for (let i = 0; i < upgrade_rows.length; i++) {
    if (available(i)) { return i }
  }
  return 0 // 呼び出し側が reward_any_available で弾いているので到達しない
}

// 上限の行は飛ばす。1 周しても見つからなければ動かさない
function move(step: number): void {
  for (let i = 0; i < upgrade_rows.length; i++) {
    selected = (selected + step + upgrade_rows.length) % upgrade_rows.length
    if (available(selected)) { break }
  }
  render()
}

function on_key(event: KeyboardEvent): void {
  const k = event.key
  if (k === 'ArrowUp' || k === 'ArrowLeft') {
    move(-1)
  } else if (k === 'ArrowDown' || k === 'ArrowRight') {
    move(1)
  } else if (k === 'Enter' && available(selected)) {
    close()
  }
}

function close(): void {
  document.removeEventListener('keydown', on_key)
  root!.style.display = 'none'
  state.boss_levels.push(upgrade_rows[selected].id)
  audio_play(audio_sfx_beep)
  state.paused = 0
  // ポーズ中も input.ts のハンドラは生きているが _update() は飛ぶので、
  // エッジ検出のフラグだけが取り残される（equip-screen.ts と同じ理由）
  keys[key_spare] = 0
  keys[key_swap] = 0
}

function render(): void {
  let rows = ''
  for (let i = 0; i < upgrade_rows.length; i++) {
    const row = upgrade_rows[i]
    const level = reward_level(row.id, meta.levels[row.id], state.boss_levels)
    const maxed = !available(i)
    const stat = maxed
      ? '<b>MAX</b>'
      : '<b>' + row.value(level) + '</b> → ' +
        '<b style="color:' + row.color + '">' + row.value(level + 1) + '</b>'
    rows += '<div class="br-row' + (i === selected ? ' on' : '') +
      (maxed ? ' maxed' : '') + '" style="color:' + row.color + '">' +
      '<img src="' + row.icon + '" alt="">' +
      '<div class="br-name">' + row.name +
      '<small>' + row.stat + ' ・ Lv. ' + level + ' / ' + meta_max_level[row.id] +
      '</small></div>' +
      '<div class="br-stat">' + stat + '</div>' +
      '</div>'
  }

  root!.innerHTML = '<div class="br-box">' +
    '<div class="br-head">灰皿撤去ユニット 停止</div>' +
    '<div class="br-sub">押収データを 1 件だけ持ち出せる（次の潜行から有効）</div>' +
    rows +
    '<div class="br-keys">[↑↓] 選ぶ　[Enter] 決定</div>' +
    '</div>'
}
```

- [ ] **Step 3: ボスの `_kill()` から呼ぶ**

`source/entity-boss.ts` の import に足す。

```ts
import { reward_any_available } from './boss-reward-model'
import { boss_reward_show } from './boss-reward'
import { meta } from './meta'
```

`_kill()` の `drop_container(this.x, this.z)` を次に差し替える。

```ts
    drop_container(this.x, this.z)

    if (reward_any_available(meta.levels, state.boss_levels)) {
      boss_reward_show()
    } else {
      // 6 本すべてが上限。まだ意味が残っている報酬は装備の段だけなので、
      // コンテナをもう 1 個落とす
      drop_container(this.x + 10, this.z)
    }
```

- [ ] **Step 4: `game.test.ts` のモックを足す**

`vi.mock('./equip-screen', ...)` の隣に追加する。追加しないと Node で `document` に触って落ちる。

```ts
vi.mock('./boss-reward', () => ({ boss_reward_show: () => {} }))
```

- [ ] **Step 5: テストと型検査を通す**

Run: `npm test && npm run typecheck && npm run build`
Expected: 全 PASS

- [ ] **Step 6: ブラウザで確かめる**

`npm run dev` を起動し、深度 5 まで潜ってボスを倒す。確認すること:

- 掃射の弾が全部同じ明るさで見える（暗い弾が混ざらない）
- 柱の陰が回転と一緒に移動する
- 撃破でダイアログが出て、`↑↓` で選べる。上限の行は飛ばされる
- `Enter` で閉じるとポーズが解け、床にコンテナが落ちている
- コンテナを踏むと開封ダイアログが出る（2 枚が同時に出ない）
- ボスを倒すまで灰皿に触れても一服が始まらない

（`docs/architecture.md`「ブラウザでの動作検証」に従うこと）

- [ ] **Step 7: commit**

```bash
git add source/boss-reward.ts source/boss-reward.css source/entity-boss.ts source/game.test.ts
git commit -m "ボス撃破で恒久強化を 1 段選ばせる"
```

---

### Task 12: ボス階の声

事実はターミナル、感情は吹き出し。同時に出さず、つぶやきは遅らせる（`docs/story.md`「声の使い分け」）。

**Files:**
- Modify: `source/monologue.ts`
- Modify: `source/game.ts`（`load_level` の到達通知）
- Modify: `source/entity-boss.ts`（撃破通知）
- Test: `source/game.test.ts`（モックの追随）

**Interfaces:**
- Consumes: `is_boss_depth`（Task 5）
- Produces: `monologue_boss_arrival(): void`, `monologue_boss_kill(): void`

- [ ] **Step 1: `monologue.ts` にプールを足す**

`lines_drone_kill` の隣に置く。

```ts
// ボス階の到達。世界の危機ではなく、灰皿の上に居座っている一点だけに反応する
const lines_boss_arrival = [
  'おい、灰皿の上に何か乗ってるぞ……',
  'でかいのが座り込んでやがる……',
  '……そこ、俺の席なんだが',
]
const lines_boss_kill = [
  'どけと言ったんだ',
  'ようやく座れる……',
  '灰皿の上に乗るんじゃねえ',
]
```

`monologue_drone_kill` の隣に関数を足す。撃破のつぶやきはターミナルの事実表示の後に出す（ドローン撃破と同じ `drone_kill_delay` を使い回さず、意味の分かる名前で同じ 2 秒を持つ）。

```ts
export function monologue_boss_arrival(): void {
  say(lines_boss_arrival, false, arrival_delay)
}
// 事実（ユニットの停止）はターミナルが先に出す。読む場所が割れないよう遅らせる
export function monologue_boss_kill(): void {
  say(lines_boss_kill, false, drone_kill_delay)
}
```

- [ ] **Step 2: `game.ts` の到達通知を分岐させる**

import に `is_boss_depth` と `monologue_boss_arrival` を足し、`load_level()` の末尾を書き換える。

```ts
  const boss_floor = is_boss_depth(depth)
  terminal_show_notice(boss_floor
    ? '深度 ' + depth + ' に到達___大型作業機の稼働音を検知'
    : '深度 ' + depth + ' に到達___喫煙所の残り香を探知中...')
  monologue_reset()
  if (boss_floor) { monologue_boss_arrival() } else { monologue_arrival() }
```

- [ ] **Step 3: ボスの撃破通知を足す**

`source/entity-boss.ts` の import に `monologue_boss_kill` と `terminal_show_notice` を足し、`_kill()` の `audio_play(audio_sfx_explode)` の直後に置く。

```ts
    terminal_show_notice('灰皿撤去ユニットの応答が途絶___区画の封鎖を解除')
    monologue_boss_kill()
```

- [ ] **Step 4: `game.test.ts` のモックを追随させる**

`vi.mock('./monologue', ...)` の返すオブジェクトに 2 つ足す。落とすと `game.ts` の import が undefined になり、フロア遷移のテストが全部落ちる。

```ts
  monologue_boss_arrival: () => {},
  monologue_boss_kill: () => {},
```

- [ ] **Step 5: テストを通す**

Run: `npm test && npm run typecheck`
Expected: 全 PASS

- [ ] **Step 6: commit**

```bash
git add source/monologue.ts source/game.ts source/entity-boss.ts source/game.test.ts
git commit -m "ボス階の到達と撃破に声を付ける"
```

---

### Task 13: ドキュメントを現状に合わせる

`docs/` は**常にコードの現状と一致させる**（`AGENTS.md`）。現在形で書き、経緯は書かない。

文面は spec（`docs/superpowers/specs/2026-08-24-boss-design.md`）の該当節から蒸留する。**実装手順・チェックボックス・移行前の状況説明は持ち込まない**（完了した時点で不要になるため）。下に引用符で示した文はそのまま使える確定文で、それ以外は spec から起こすこと。

**Files:**
- Modify: `docs/enemies.md`, `docs/gameplay.md`, `docs/meta-progression.md`, `docs/equipment.md`, `docs/architecture.md`, `docs/story.md`
- Delete: `docs/superpowers/specs/2026-08-24-boss-design.md`, `docs/superpowers/plans/2026-08-24-boss.md`

**Interfaces:**
- Consumes: なし
- Produces: なし

- [ ] **Step 1: `docs/enemies.md`**

冒頭を「蜘蛛・セントリー・清掃ドローンの 3 体と、ボス階に 1 体」に直す。役割分担の表にボス（`entity-boss.ts`、耐久 `40 + 4 × 深度`）の行を足し、「灰皿に居座って一服を塞ぐ」を役割として書く。

「3 体に共通する規約」の**シェイクの序列**を書き換える。

> **カメラシェイクの序列は 蜘蛛 1 < セントリー 3 < 銘品の開封 4 < 自機の死 5 < 清掃ドローン 6 < ボス撃破 8。**

新しい節「ボス（灰皿撤去ユニット）」を末尾に足す。書くのは、掃射が時間ではなく掃引角で刻まれる理由、回転する安全地帯と柱の関係、砲口の本数だけで深度スケールする理由、弾がライトを積まず full-bright で描かれる理由（同時 26 発と `max_lights = 16`）、刃物の一撃必殺を効かせない理由、撃破時に残弾を消す理由。

- [ ] **Step 2: `docs/gameplay.md`**

「ポーズは装備の開封だけ」を「ポーズは開封とボス報酬の 2 つ」に改題し、どちらも `state.paused` を立てること、フラグを 2 本持たない理由を書く。

「ダミー喫煙所」に 1 段落足す。

> ボス階（深度が 5 の倍数）にはダミーを置かないので、深度テーブルの実効値は深度 6〜9 で 1、11〜14 で 2、16 以降で 3 になり、5 の倍数ちょうどでは 0 である。式は変えていない。

新しい節「ボス階」を「ダミー喫煙所」の後に足す。書くのは、中核の問いが「見つけられるか」から「空けられるか」へ置き換わること、一辺 23 タイル固定の 2 つの理由（霧 112px と間合いの一貫性）、柱をリングにする理由、置かないもの（雑魚・ダミー・床のヤニ・清掃ドローン）とそれぞれの理由、開始と非常口が対角であること。

同じ節の末尾に、**分岐を足していない**ことも明記する（足すべきだったのに忘れた、と後から読めてしまうため）。

> ミニマップと嗅覚にボス階の分岐は無い。中央の喫煙所は未訪問なので通常どおりオレンジで明滅し、それがそのままボスの位置でもある。嗅覚は目標リストが 1 点になるだけで、位置を教える能力が位置の既知な階で無力化されるのは設計どおりである（`docs/meta-progression.md`）。

- [ ] **Step 3: `docs/meta-progression.md`**

「通貨「ヤニ」」の後に新しい節「ボス報酬」を足す。書くのは、5 階ごとに 1 段が無償で入ること、`state.boss_levels` に積んで `run_end()` がヤニと同じブロックで書くこと、その理由（ラン中に `meta.levels` を書くと毎フレーム読む 4 本だけが先に効く）、寿命がヤニと完全に一致すること、6 本すべてが上限のときはコンテナに化けること。

- [ ] **Step 4: `docs/equipment.md`**

コンテナの出現の節に足す。

> ボス（`docs/enemies.md`）は撃破で必ず 1 個落とす。段は `gear_roll_tier` を 2 回引いて高いほうを採る（best-of-2）。段を底上げする専用テーブルを持たせると「段の抽選は深度で重み付けられる」が壊れるが、best-of-2 なら重み付けの形はそのままで期待値だけが上がる。

- [ ] **Step 5: `docs/architecture.md`**

モジュール構成の一覧に 5 行足す（`boss-model` / `entity-boss` / `boss-reward-model` / `boss-reward` / `upgrade-rows`）。`boss-model` と `boss-reward-model` は「実行時 import を持たない」の一覧にも入れる（`boss-reward-model` は `meta` を 1 本持つので、「Node で評価できる」側だけに入れること）。

「アトラスの焼き込み」に足す。

> ボス（45）と弾（46）は `tools/boss_tiles.py` が焼く。46 は絵ではなく単色の点なので `slash_tiles.py` と同じくコードが原本になり、色は既存の敵の赤 `(255,66,0)` —— full-bright 規則（`r>0.95 && g>0.25 && b==0`）を満たす唯一の帯 —— を使う。45 は `tools/atlas.py` が焼く画像で、`TILE_RANGE` は 33〜46。

新しい節「スプライトの大きさ」を足し、`push_sprite()` が 6 固定であること、大きいものは `push_quad()` を直に呼ぶこと、`entity_t.w` が衝突判定の一辺で既定 9 であること、見た目と判定を別々に持つ理由を書く。

- [ ] **Step 6: `docs/story.md`**

ゲーム進行との対応の表に 1 行足す。

> | ボス | 5 階ごとに降りてきて灰皿を撤去して回る大型作業機 |

ダミー喫煙所の行を「灰皿だけ撤去された跡地。**このボスが既に仕事を終えた場所**」に直す。

- [ ] **Step 7: 作業用ドキュメントを消す**

設計の結論は `docs/` 直下へ蒸留済みなので、spec と plan を削除する（`AGENTS.md`）。

```bash
git rm docs/superpowers/specs/2026-08-24-boss-design.md docs/superpowers/plans/2026-08-24-boss.md
```

- [ ] **Step 8: 記述がコードと合っているか確かめる**

Run: `npm test && npm run typecheck && npm run build`
Expected: 全 PASS

数値を書いた箇所（耐久の式・砲口の本数・一辺 23・シェイク 8・タイル 45/46）は `source/boss-model.ts` と `source/level-generator.ts` を開いて 1 つずつ突き合わせること。

- [ ] **Step 9: commit**

```bash
git add docs/
git commit -m "ボス戦をドキュメントに反映する"
```

---

### Task 14: ボスの絵を焼き込む

`images.md` のプロンプトで生成した画像を 45 番に焼き、暫定のプレースホルダを消す。**この作業は絵が用意できてから行う。**

**Files:**
- Modify: `m/q2.png`
- Modify: `tools/boss_tiles.py`（プレースホルダの関数を削除）
- Delete: `images.md`

**Interfaces:**
- Consumes: `images.md` のプロンプトで生成した `45.png`
- Produces: なし

- [ ] **Step 1: 生成した画像を焼く**

生成した PNG を作業用ディレクトリに `45.png` として置き、実行する。

```bash
uv run --with pillow python tools/atlas.py <45.png を置いたディレクトリ>
```
Expected: `baked tiles [45] into .../m/q2.png`

- [ ] **Step 2: 背景キーが抜けたか確かめる**

```bash
uv run --with pillow python -c "
from PIL import Image
im = Image.open('m/q2.png').convert('RGBA')
t = im.crop((45*16,0,46*16,16))
px = [p for p in t.get_flattened_data() if p[3] >= 204]
fb = [p for p in px if p[0] > 242 and p[1] > 63 and p[2] == 0]
print('opaque', len(px), 'full-bright', len(fb))
"
```
Expected: `opaque` が 256 未満（＝四隅の背景が透過している）で、`full-bright` が 1 以上（＝目が光る色で描かれている）

- [ ] **Step 3: ブラウザで見る**

`npm run dev` で深度 5 まで潜り、ボスが灰皿の上に立って通常の敵より明らかに大きく見えること、目が暗がりでも光ることを確認する。

- [ ] **Step 4: プレースホルダを消す**

`tools/boss_tiles.py` から `bake_boss_placeholder()` と定数 `BODY` / `EYE` / `BOSS_TILE`、`main()` の呼び出しを削除する。docstring から 45 に関する段落を削り、「46（ボスの弾）だけを焼く」に直す。ファイル名は変えない。

Run: `uv run --with pillow python tools/boss_tiles.py`
Expected: `baked tile 46 into .../m/q2.png`。45 が上書きされていないことを Step 2 のコマンドで再確認する

- [ ] **Step 5: `images.md` を消す**

役目を終えたので削除する（押収品コンテナのときと同じ手順）。

```bash
git rm images.md
```

- [ ] **Step 6: commit**

```bash
git add m/q2.png tools/boss_tiles.py
git commit -m "ボスの絵をアトラス 45 に焼き込む"
```
